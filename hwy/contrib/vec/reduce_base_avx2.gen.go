// Code generated by github.com/ajroetker/go-highway/cmd/hwygen. DO NOT EDIT.

//go:build amd64 && goexperiment.simd

package vec

import (
	"simd/archsimd"
	"unsafe"

	"github.com/ajroetker/go-highway/hwy"
	"github.com/ajroetker/go-highway/hwy/asm"
)

func BaseSum_avx2_Float16(v []hwy.Float16) hwy.Float16 {
	if len(v) == 0 {
		return 0
	}
	sum := asm.ZeroFloat16x8AVX2()
	lanes := 8
	var i int
	for i = 0; i+lanes*2 <= len(v); i += lanes * 2 {
		va := asm.LoadFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(v[i:]))), len(v[i:])))
		sum = sum.Add(va)
		va1 := asm.LoadFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(v[i+8:]))), len(v[i+8:])))
		sum = sum.Add(va1)
	}
	result := sum.ReduceSum()
	for ; i < len(v); i++ {
		result += v[i].Float32()
	}
	return hwy.Float32ToFloat16(result)
}

func BaseSum_avx2_BFloat16(v []hwy.BFloat16) hwy.BFloat16 {
	if len(v) == 0 {
		return 0
	}
	sum := asm.ZeroBFloat16x8AVX2()
	lanes := 8
	var i int
	for i = 0; i+lanes*2 <= len(v); i += lanes * 2 {
		va := asm.LoadBFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(v[i:]))), len(v[i:])))
		sum = sum.Add(va)
		va1 := asm.LoadBFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(v[i+8:]))), len(v[i+8:])))
		sum = sum.Add(va1)
	}
	result := sum.ReduceSum()
	for ; i < len(v); i++ {
		result += v[i].Float32()
	}
	return hwy.Float32ToBFloat16(result)
}

func BaseSum_avx2(v []float32) float32 {
	if len(v) == 0 {
		return 0
	}
	sum := archsimd.BroadcastFloat32x8(0)
	lanes := 8
	var i int
	for i = 0; i+lanes*2 <= len(v); i += lanes * 2 {
		va := archsimd.LoadFloat32x8((*[8]float32)(unsafe.Pointer(&v[i])))
		sum = sum.Add(va)
		va1 := archsimd.LoadFloat32x8((*[8]float32)(unsafe.Pointer(&v[i+8])))
		sum = sum.Add(va1)
	}
	result := hwy.ReduceSum_AVX2_F32x8(sum)
	for ; i < len(v); i++ {
		result += v[i]
	}
	return result
}

func BaseSum_avx2_Float64(v []float64) float64 {
	if len(v) == 0 {
		return 0
	}
	sum := archsimd.BroadcastFloat64x4(0)
	lanes := 4
	var i int
	for i = 0; i+lanes*2 <= len(v); i += lanes * 2 {
		va := archsimd.LoadFloat64x4((*[4]float64)(unsafe.Pointer(&v[i])))
		sum = sum.Add(va)
		va1 := archsimd.LoadFloat64x4((*[4]float64)(unsafe.Pointer(&v[i+4])))
		sum = sum.Add(va1)
	}
	result := hwy.ReduceSum_AVX2_F64x4(sum)
	for ; i < len(v); i++ {
		result += v[i]
	}
	return result
}

func BaseMin_avx2_Float16(v []hwy.Float16) hwy.Float16 {
	if len(v) == 0 {
		panic("vec: Min called on empty slice")
	}
	lanes := 8
	if len(v) < lanes {
		result := v[0]
		for i := 1; i < len(v); i++ {
			if v[i].Float32() < result.Float32() {
				result = v[i]
			}
		}
		return result
	}
	minVec := asm.LoadFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(v))), len(v)))
	var i int
	for i = lanes; i+lanes*2 <= len(v); i += lanes * 2 {
		va := asm.LoadFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(v[i:]))), len(v[i:])))
		minVec = minVec.Min(va)
		va1 := asm.LoadFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(v[i+8:]))), len(v[i+8:])))
		minVec = minVec.Min(va1)
	}
	result := minVec.ReduceMin()
	for ; i < len(v); i++ {
		if v[i].Float32() < result {
			result = v[i].Float32()
		}
	}
	return hwy.Float32ToFloat16(result)
}

func BaseMin_avx2_BFloat16(v []hwy.BFloat16) hwy.BFloat16 {
	if len(v) == 0 {
		panic("vec: Min called on empty slice")
	}
	lanes := 8
	if len(v) < lanes {
		result := v[0]
		for i := 1; i < len(v); i++ {
			if v[i].Float32() < result.Float32() {
				result = v[i]
			}
		}
		return result
	}
	minVec := asm.LoadBFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(v))), len(v)))
	var i int
	for i = lanes; i+lanes*2 <= len(v); i += lanes * 2 {
		va := asm.LoadBFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(v[i:]))), len(v[i:])))
		minVec = minVec.Min(va)
		va1 := asm.LoadBFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(v[i+8:]))), len(v[i+8:])))
		minVec = minVec.Min(va1)
	}
	result := minVec.ReduceMin()
	for ; i < len(v); i++ {
		if v[i].Float32() < result {
			result = v[i].Float32()
		}
	}
	return hwy.Float32ToBFloat16(result)
}

func BaseMin_avx2(v []float32) float32 {
	if len(v) == 0 {
		panic("vec: Min called on empty slice")
	}
	lanes := 8
	if len(v) < lanes {
		result := v[0]
		for i := 1; i < len(v); i++ {
			if v[i] < result {
				result = v[i]
			}
		}
		return result
	}
	minVec := archsimd.LoadFloat32x8Slice(v)
	var i int
	for i = lanes; i+lanes*2 <= len(v); i += lanes * 2 {
		va := archsimd.LoadFloat32x8((*[8]float32)(unsafe.Pointer(&v[i])))
		minVec = minVec.Min(va)
		va1 := archsimd.LoadFloat32x8((*[8]float32)(unsafe.Pointer(&v[i+8])))
		minVec = minVec.Min(va1)
	}
	result := hwy.ReduceMin_AVX2_F32x8(minVec)
	for ; i < len(v); i++ {
		if v[i] < result {
			result = v[i]
		}
	}
	return result
}

func BaseMin_avx2_Float64(v []float64) float64 {
	if len(v) == 0 {
		panic("vec: Min called on empty slice")
	}
	lanes := 4
	if len(v) < lanes {
		result := v[0]
		for i := 1; i < len(v); i++ {
			if v[i] < result {
				result = v[i]
			}
		}
		return result
	}
	minVec := archsimd.LoadFloat64x4Slice(v)
	var i int
	for i = lanes; i+lanes*2 <= len(v); i += lanes * 2 {
		va := archsimd.LoadFloat64x4((*[4]float64)(unsafe.Pointer(&v[i])))
		minVec = minVec.Min(va)
		va1 := archsimd.LoadFloat64x4((*[4]float64)(unsafe.Pointer(&v[i+4])))
		minVec = minVec.Min(va1)
	}
	result := hwy.ReduceMin_AVX2_F64x4(minVec)
	for ; i < len(v); i++ {
		if v[i] < result {
			result = v[i]
		}
	}
	return result
}

func BaseMax_avx2(v []float32) float32 {
	if len(v) == 0 {
		panic("vec: Max called on empty slice")
	}
	lanes := 8
	if len(v) < lanes {
		result := v[0]
		for i := 1; i < len(v); i++ {
			if v[i] > result {
				result = v[i]
			}
		}
		return result
	}
	maxVec := archsimd.LoadFloat32x8Slice(v)
	var i int
	for i = lanes; i+lanes*2 <= len(v); i += lanes * 2 {
		va := archsimd.LoadFloat32x8((*[8]float32)(unsafe.Pointer(&v[i])))
		maxVec = maxVec.Max(va)
		va1 := archsimd.LoadFloat32x8((*[8]float32)(unsafe.Pointer(&v[i+8])))
		maxVec = maxVec.Max(va1)
	}
	result := hwy.ReduceMax_AVX2_F32x8(maxVec)
	for ; i < len(v); i++ {
		if v[i] > result {
			result = v[i]
		}
	}
	return result
}

func BaseMax_avx2_Float64(v []float64) float64 {
	if len(v) == 0 {
		panic("vec: Max called on empty slice")
	}
	lanes := 4
	if len(v) < lanes {
		result := v[0]
		for i := 1; i < len(v); i++ {
			if v[i] > result {
				result = v[i]
			}
		}
		return result
	}
	maxVec := archsimd.LoadFloat64x4Slice(v)
	var i int
	for i = lanes; i+lanes*2 <= len(v); i += lanes * 2 {
		va := archsimd.LoadFloat64x4((*[4]float64)(unsafe.Pointer(&v[i])))
		maxVec = maxVec.Max(va)
		va1 := archsimd.LoadFloat64x4((*[4]float64)(unsafe.Pointer(&v[i+4])))
		maxVec = maxVec.Max(va1)
	}
	result := hwy.ReduceMax_AVX2_F64x4(maxVec)
	for ; i < len(v); i++ {
		if v[i] > result {
			result = v[i]
		}
	}
	return result
}

func BaseMax_avx2_Int32(v []int32) int32 {
	if len(v) == 0 {
		panic("vec: Max called on empty slice")
	}
	lanes := 8
	if len(v) < lanes {
		result := v[0]
		for i := 1; i < len(v); i++ {
			if v[i] > result {
				result = v[i]
			}
		}
		return result
	}
	maxVec := archsimd.LoadInt32x8Slice(v)
	var i int
	for i = lanes; i+lanes*2 <= len(v); i += lanes * 2 {
		va := archsimd.LoadInt32x8((*[8]int32)(unsafe.Pointer(&v[i])))
		maxVec = maxVec.Max(va)
		va1 := archsimd.LoadInt32x8((*[8]int32)(unsafe.Pointer(&v[i+8])))
		maxVec = maxVec.Max(va1)
	}
	result := hwy.ReduceMax_AVX2_I32x8(maxVec)
	for ; i < len(v); i++ {
		if v[i] > result {
			result = v[i]
		}
	}
	return result
}

func BaseMax_avx2_Int64(v []int64) int64 {
	if len(v) == 0 {
		panic("vec: Max called on empty slice")
	}
	lanes := 4
	if len(v) < lanes {
		result := v[0]
		for i := 1; i < len(v); i++ {
			if v[i] > result {
				result = v[i]
			}
		}
		return result
	}
	maxVec := archsimd.LoadInt64x4Slice(v)
	var i int
	for i = lanes; i+lanes*2 <= len(v); i += lanes * 2 {
		va := archsimd.LoadInt64x4((*[4]int64)(unsafe.Pointer(&v[i])))
		maxVec = hwy.Max_AVX2_Int64x4(maxVec, va)
		va1 := archsimd.LoadInt64x4((*[4]int64)(unsafe.Pointer(&v[i+4])))
		maxVec = hwy.Max_AVX2_Int64x4(maxVec, va1)
	}
	result := hwy.ReduceMax_AVX2_I64x4(maxVec)
	for ; i < len(v); i++ {
		if v[i] > result {
			result = v[i]
		}
	}
	return result
}

func BaseMax_avx2_Uint32(v []uint32) uint32 {
	if len(v) == 0 {
		panic("vec: Max called on empty slice")
	}
	lanes := 8
	if len(v) < lanes {
		result := v[0]
		for i := 1; i < len(v); i++ {
			if v[i] > result {
				result = v[i]
			}
		}
		return result
	}
	maxVec := archsimd.LoadUint32x8Slice(v)
	var i int
	for i = lanes; i+lanes*2 <= len(v); i += lanes * 2 {
		va := archsimd.LoadUint32x8((*[8]uint32)(unsafe.Pointer(&v[i])))
		maxVec = maxVec.Max(va)
		va1 := archsimd.LoadUint32x8((*[8]uint32)(unsafe.Pointer(&v[i+8])))
		maxVec = maxVec.Max(va1)
	}
	result := hwy.ReduceMax_AVX2_Uint32x8(maxVec)
	for ; i < len(v); i++ {
		if v[i] > result {
			result = v[i]
		}
	}
	return result
}

func BaseMax_avx2_Uint64(v []uint64) uint64 {
	if len(v) == 0 {
		panic("vec: Max called on empty slice")
	}
	lanes := 4
	if len(v) < lanes {
		result := v[0]
		for i := 1; i < len(v); i++ {
			if v[i] > result {
				result = v[i]
			}
		}
		return result
	}
	maxVec := archsimd.LoadUint64x4Slice(v)
	var i int
	for i = lanes; i+lanes*2 <= len(v); i += lanes * 2 {
		va := archsimd.LoadUint64x4((*[4]uint64)(unsafe.Pointer(&v[i])))
		maxVec = hwy.Max_AVX2_Uint64x4(maxVec, va)
		va1 := archsimd.LoadUint64x4((*[4]uint64)(unsafe.Pointer(&v[i+4])))
		maxVec = hwy.Max_AVX2_Uint64x4(maxVec, va1)
	}
	result := hwy.ReduceMax_AVX2_Uint64x4(maxVec)
	for ; i < len(v); i++ {
		if v[i] > result {
			result = v[i]
		}
	}
	return result
}

func BaseMinMax_avx2_Float16(v []hwy.Float16) (minVal hwy.Float16, maxVal hwy.Float16) {
	if len(v) == 0 {
		panic("vec: MinMax called on empty slice")
	}
	lanes := 8
	if len(v) < lanes {
		minVal = v[0]
		maxVal = v[0]
		for i := 1; i < len(v); i++ {
			if v[i].Float32() < minVal.Float32() {
				minVal = v[i]
			}
			if v[i].Float32() > maxVal.Float32() {
				maxVal = v[i]
			}
		}
		return minVal, maxVal
	}
	minVec := asm.LoadFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(v))), len(v)))
	maxVec := minVec
	var i int
	for i = lanes; i+lanes*2 <= len(v); i += lanes * 2 {
		va := asm.LoadFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(v[i:]))), len(v[i:])))
		minVec = minVec.Min(va)
		maxVec = maxVec.Max(va)
		va1 := asm.LoadFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(v[i+8:]))), len(v[i+8:])))
		minVec = minVec.Min(va1)
		maxVec = maxVec.Max(va1)
	}
	minVal = hwy.Float32ToFloat16(minVec.ReduceMin())
	maxVal = hwy.Float32ToFloat16(maxVec.ReduceMax())
	for ; i < len(v); i++ {
		if v[i].Float32() < minVal.Float32() {
			minVal = v[i]
		}
		if v[i].Float32() > maxVal.Float32() {
			maxVal = v[i]
		}
	}
	return minVal, maxVal
}

func BaseMinMax_avx2_BFloat16(v []hwy.BFloat16) (minVal hwy.BFloat16, maxVal hwy.BFloat16) {
	if len(v) == 0 {
		panic("vec: MinMax called on empty slice")
	}
	lanes := 8
	if len(v) < lanes {
		minVal = v[0]
		maxVal = v[0]
		for i := 1; i < len(v); i++ {
			if v[i].Float32() < minVal.Float32() {
				minVal = v[i]
			}
			if v[i].Float32() > maxVal.Float32() {
				maxVal = v[i]
			}
		}
		return minVal, maxVal
	}
	minVec := asm.LoadBFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(v))), len(v)))
	maxVec := minVec
	var i int
	for i = lanes; i+lanes*2 <= len(v); i += lanes * 2 {
		va := asm.LoadBFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(v[i:]))), len(v[i:])))
		minVec = minVec.Min(va)
		maxVec = maxVec.Max(va)
		va1 := asm.LoadBFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(v[i+8:]))), len(v[i+8:])))
		minVec = minVec.Min(va1)
		maxVec = maxVec.Max(va1)
	}
	minVal = hwy.Float32ToBFloat16(minVec.ReduceMin())
	maxVal = hwy.Float32ToBFloat16(maxVec.ReduceMax())
	for ; i < len(v); i++ {
		if v[i].Float32() < minVal.Float32() {
			minVal = v[i]
		}
		if v[i].Float32() > maxVal.Float32() {
			maxVal = v[i]
		}
	}
	return minVal, maxVal
}

func BaseMinMax_avx2(v []float32) (minVal float32, maxVal float32) {
	if len(v) == 0 {
		panic("vec: MinMax called on empty slice")
	}
	lanes := 8
	if len(v) < lanes {
		minVal = v[0]
		maxVal = v[0]
		for i := 1; i < len(v); i++ {
			if v[i] < minVal {
				minVal = v[i]
			}
			if v[i] > maxVal {
				maxVal = v[i]
			}
		}
		return minVal, maxVal
	}
	minVec := archsimd.LoadFloat32x8Slice(v)
	maxVec := minVec
	var i int
	for i = lanes; i+lanes*2 <= len(v); i += lanes * 2 {
		va := archsimd.LoadFloat32x8((*[8]float32)(unsafe.Pointer(&v[i])))
		minVec = minVec.Min(va)
		maxVec = maxVec.Max(va)
		va1 := archsimd.LoadFloat32x8((*[8]float32)(unsafe.Pointer(&v[i+8])))
		minVec = minVec.Min(va1)
		maxVec = maxVec.Max(va1)
	}
	minVal = hwy.ReduceMin_AVX2_F32x8(minVec)
	maxVal = hwy.ReduceMax_AVX2_F32x8(maxVec)
	for ; i < len(v); i++ {
		if v[i] < minVal {
			minVal = v[i]
		}
		if v[i] > maxVal {
			maxVal = v[i]
		}
	}
	return minVal, maxVal
}

func BaseMinMax_avx2_Float64(v []float64) (minVal float64, maxVal float64) {
	if len(v) == 0 {
		panic("vec: MinMax called on empty slice")
	}
	lanes := 4
	if len(v) < lanes {
		minVal = v[0]
		maxVal = v[0]
		for i := 1; i < len(v); i++ {
			if v[i] < minVal {
				minVal = v[i]
			}
			if v[i] > maxVal {
				maxVal = v[i]
			}
		}
		return minVal, maxVal
	}
	minVec := archsimd.LoadFloat64x4Slice(v)
	maxVec := minVec
	var i int
	for i = lanes; i+lanes*2 <= len(v); i += lanes * 2 {
		va := archsimd.LoadFloat64x4((*[4]float64)(unsafe.Pointer(&v[i])))
		minVec = minVec.Min(va)
		maxVec = maxVec.Max(va)
		va1 := archsimd.LoadFloat64x4((*[4]float64)(unsafe.Pointer(&v[i+4])))
		minVec = minVec.Min(va1)
		maxVec = maxVec.Max(va1)
	}
	minVal = hwy.ReduceMin_AVX2_F64x4(minVec)
	maxVal = hwy.ReduceMax_AVX2_F64x4(maxVec)
	for ; i < len(v); i++ {
		if v[i] < minVal {
			minVal = v[i]
		}
		if v[i] > maxVal {
			maxVal = v[i]
		}
	}
	return minVal, maxVal
}
