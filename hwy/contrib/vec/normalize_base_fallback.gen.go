// Code generated by github.com/ajroetker/go-highway/cmd/hwygen. DO NOT EDIT.

package vec

import (
	stdmath "math"

	"github.com/ajroetker/go-highway/hwy"
)

func BaseNormalize_fallback_Float16(dst []hwy.Float16) {
	if len(dst) == 0 {
		return
	}
	squaredNorm := BaseDot_fallback_Float16(dst, dst)
	if squaredNorm == 0 {
		return
	}
	norm := float32(stdmath.Sqrt(float64(squaredNorm)))
	scale := float32(1) / norm
	scaleVec := hwy.Set(hwy.Float32ToFloat16(scale))
	lanes := scaleVec.NumLanes()
	var i int
	for i = 0; i+lanes <= len(dst); i += lanes {
		vec := hwy.LoadFull(dst[i:])
		result := hwy.Mul(vec, scaleVec)
		hwy.StoreFull(result, dst[i:])
	}
	for ; i < len(dst); i++ {
		dst[i] = hwy.Float32ToFloat16(dst[i].Float32() * scale)
	}
}

func BaseNormalize_fallback_BFloat16(dst []hwy.BFloat16) {
	if len(dst) == 0 {
		return
	}
	squaredNorm := BaseDot_fallback_BFloat16(dst, dst)
	if squaredNorm == 0 {
		return
	}
	norm := float32(stdmath.Sqrt(float64(squaredNorm)))
	scale := float32(1) / norm
	scaleVec := hwy.Set(hwy.Float32ToBFloat16(scale))
	lanes := scaleVec.NumLanes()
	var i int
	for i = 0; i+lanes <= len(dst); i += lanes {
		vec := hwy.LoadFull(dst[i:])
		result := hwy.Mul(vec, scaleVec)
		hwy.StoreFull(result, dst[i:])
	}
	for ; i < len(dst); i++ {
		dst[i] = hwy.Float32ToBFloat16(dst[i].Float32() * scale)
	}
}

func BaseNormalize_fallback(dst []float32) {
	if len(dst) == 0 {
		return
	}
	squaredNorm := BaseDot_fallback(dst, dst)
	if squaredNorm == 0 {
		return
	}
	norm := float32(stdmath.Sqrt(float64(squaredNorm)))
	scale := float32(1) / norm
	scaleVec := float32(scale)
	var i int
	for i = 0; i < len(dst); i++ {
		vec := dst[i]
		result := vec * scaleVec
		dst[i] = result
	}
	for ; i < len(dst); i++ {
		dst[i] *= scale
	}
}

func BaseNormalize_fallback_Float64(dst []float64) {
	if len(dst) == 0 {
		return
	}
	squaredNorm := BaseDot_fallback_Float64(dst, dst)
	if squaredNorm == 0 {
		return
	}
	norm := float64(stdmath.Sqrt(float64(squaredNorm)))
	scale := float64(1) / norm
	scaleVec := float64(scale)
	var i int
	for i = 0; i < len(dst); i++ {
		vec := dst[i]
		result := vec * scaleVec
		dst[i] = result
	}
	for ; i < len(dst); i++ {
		dst[i] *= scale
	}
}

func BaseNormalizeTo_fallback_Float16(dst []hwy.Float16, src []hwy.Float16) {
	n := min(len(dst), len(src))
	if n == 0 {
		return
	}
	squaredNorm := BaseDot_fallback_Float16(src[:n], src[:n])
	if squaredNorm == 0 {
		copy(dst[:n], src[:n])
		return
	}
	norm := float32(stdmath.Sqrt(float64(squaredNorm)))
	scale := float32(1) / norm
	scaleVec := hwy.Set(hwy.Float32ToFloat16(scale))
	lanes := scaleVec.NumLanes()
	var i int
	for i = 0; i+lanes <= n; i += lanes {
		vec := hwy.LoadFull(src[i:])
		result := hwy.Mul(vec, scaleVec)
		hwy.StoreFull(result, dst[i:])
	}
	for ; i < n; i++ {
		dst[i] = hwy.Float32ToFloat16(src[i].Float32() * scale)
	}
}

func BaseNormalizeTo_fallback_BFloat16(dst []hwy.BFloat16, src []hwy.BFloat16) {
	n := min(len(dst), len(src))
	if n == 0 {
		return
	}
	squaredNorm := BaseDot_fallback_BFloat16(src[:n], src[:n])
	if squaredNorm == 0 {
		copy(dst[:n], src[:n])
		return
	}
	norm := float32(stdmath.Sqrt(float64(squaredNorm)))
	scale := float32(1) / norm
	scaleVec := hwy.Set(hwy.Float32ToBFloat16(scale))
	lanes := scaleVec.NumLanes()
	var i int
	for i = 0; i+lanes <= n; i += lanes {
		vec := hwy.LoadFull(src[i:])
		result := hwy.Mul(vec, scaleVec)
		hwy.StoreFull(result, dst[i:])
	}
	for ; i < n; i++ {
		dst[i] = hwy.Float32ToBFloat16(src[i].Float32() * scale)
	}
}

func BaseNormalizeTo_fallback(dst []float32, src []float32) {
	n := min(len(dst), len(src))
	if n == 0 {
		return
	}
	squaredNorm := BaseDot_fallback(src[:n], src[:n])
	if squaredNorm == 0 {
		copy(dst[:n], src[:n])
		return
	}
	norm := float32(stdmath.Sqrt(float64(squaredNorm)))
	scale := float32(1) / norm
	scaleVec := float32(scale)
	var i int
	for i = 0; i < n; i++ {
		vec := src[i]
		result := vec * scaleVec
		dst[i] = result
	}
	for ; i < n; i++ {
		dst[i] = src[i] * scale
	}
}

func BaseNormalizeTo_fallback_Float64(dst []float64, src []float64) {
	n := min(len(dst), len(src))
	if n == 0 {
		return
	}
	squaredNorm := BaseDot_fallback_Float64(src[:n], src[:n])
	if squaredNorm == 0 {
		copy(dst[:n], src[:n])
		return
	}
	norm := float64(stdmath.Sqrt(float64(squaredNorm)))
	scale := float64(1) / norm
	scaleVec := float64(scale)
	var i int
	for i = 0; i < n; i++ {
		vec := src[i]
		result := vec * scaleVec
		dst[i] = result
	}
	for ; i < n; i++ {
		dst[i] = src[i] * scale
	}
}
