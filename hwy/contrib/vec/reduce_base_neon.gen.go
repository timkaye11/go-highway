// Code generated by github.com/ajroetker/go-highway/cmd/hwygen. DO NOT EDIT.

//go:build arm64

package vec

import (
	"unsafe"

	"github.com/ajroetker/go-highway/hwy"
	"github.com/ajroetker/go-highway/hwy/asm"
)

func BaseSum_neon_Float16(v []hwy.Float16) hwy.Float16 {
	if len(v) == 0 {
		return 0
	}
	sum := asm.ZeroFloat16x8()
	lanes := 8
	var i int
	for i = 0; i+lanes*2 <= len(v); i += lanes * 2 {
		va := asm.LoadFloat16x8Ptr(unsafe.Pointer(&v[i:][0]))
		sum = sum.Add(va)
		va1 := asm.LoadFloat16x8Ptr(unsafe.Pointer(&v[i+8:][0]))
		sum = sum.Add(va1)
	}
	result := sum.ReduceSum()
	for ; i < len(v); i++ {
		result += v[i].Float32()
	}
	return hwy.Float32ToFloat16(result)
}

func BaseSum_neon_BFloat16(v []hwy.BFloat16) hwy.BFloat16 {
	if len(v) == 0 {
		return 0
	}
	sum := asm.ZeroBFloat16x8()
	lanes := 8
	var i int
	for i = 0; i+lanes*2 <= len(v); i += lanes * 2 {
		va := asm.LoadBFloat16x8Ptr(unsafe.Pointer(&v[i:][0]))
		sum = sum.Add(va)
		va1 := asm.LoadBFloat16x8Ptr(unsafe.Pointer(&v[i+8:][0]))
		sum = sum.Add(va1)
	}
	result := sum.ReduceSum()
	for ; i < len(v); i++ {
		result += v[i].Float32()
	}
	return hwy.Float32ToBFloat16(result)
}

func BaseSum_neon(v []float32) float32 {
	if len(v) == 0 {
		return 0
	}
	sum := asm.ZeroFloat32x4()
	lanes := 4
	var i int
	for i = 0; i+lanes*2 <= len(v); i += lanes * 2 {
		va := asm.LoadFloat32x4((*[4]float32)(unsafe.Pointer(&v[i])))
		sum = sum.Add(va)
		va1 := asm.LoadFloat32x4((*[4]float32)(unsafe.Pointer(&v[i+4])))
		sum = sum.Add(va1)
	}
	result := sum.ReduceSum()
	for ; i < len(v); i++ {
		result += v[i]
	}
	return result
}

func BaseSum_neon_Float64(v []float64) float64 {
	if len(v) == 0 {
		return 0
	}
	sum := asm.ZeroFloat64x2()
	lanes := 2
	var i int
	for i = 0; i+lanes*2 <= len(v); i += lanes * 2 {
		va := asm.LoadFloat64x2((*[2]float64)(unsafe.Pointer(&v[i])))
		sum = sum.Add(va)
		va1 := asm.LoadFloat64x2((*[2]float64)(unsafe.Pointer(&v[i+2])))
		sum = sum.Add(va1)
	}
	result := sum.ReduceSum()
	for ; i < len(v); i++ {
		result += v[i]
	}
	return result
}

func BaseMin_neon_Float16(v []hwy.Float16) hwy.Float16 {
	if len(v) == 0 {
		panic("vec: Min called on empty slice")
	}
	lanes := 8
	if len(v) < lanes {
		result := v[0]
		for i := 1; i < len(v); i++ {
			if v[i].Float32() < result.Float32() {
				result = v[i]
			}
		}
		return result
	}
	minVec := asm.LoadFloat16x8Ptr(unsafe.Pointer(&v[0]))
	var i int
	for i = lanes; i+lanes*2 <= len(v); i += lanes * 2 {
		va := asm.LoadFloat16x8Ptr(unsafe.Pointer(&v[i:][0]))
		minVec = minVec.Min(va)
		va1 := asm.LoadFloat16x8Ptr(unsafe.Pointer(&v[i+8:][0]))
		minVec = minVec.Min(va1)
	}
	result := minVec.ReduceMin()
	for ; i < len(v); i++ {
		if v[i].Float32() < result {
			result = v[i].Float32()
		}
	}
	return hwy.Float32ToFloat16(result)
}

func BaseMin_neon_BFloat16(v []hwy.BFloat16) hwy.BFloat16 {
	if len(v) == 0 {
		panic("vec: Min called on empty slice")
	}
	lanes := 8
	if len(v) < lanes {
		result := v[0]
		for i := 1; i < len(v); i++ {
			if v[i].Float32() < result.Float32() {
				result = v[i]
			}
		}
		return result
	}
	minVec := asm.LoadBFloat16x8Ptr(unsafe.Pointer(&v[0]))
	var i int
	for i = lanes; i+lanes*2 <= len(v); i += lanes * 2 {
		va := asm.LoadBFloat16x8Ptr(unsafe.Pointer(&v[i:][0]))
		minVec = minVec.Min(va)
		va1 := asm.LoadBFloat16x8Ptr(unsafe.Pointer(&v[i+8:][0]))
		minVec = minVec.Min(va1)
	}
	result := minVec.ReduceMin()
	for ; i < len(v); i++ {
		if v[i].Float32() < result {
			result = v[i].Float32()
		}
	}
	return hwy.Float32ToBFloat16(result)
}

func BaseMin_neon(v []float32) float32 {
	if len(v) == 0 {
		panic("vec: Min called on empty slice")
	}
	lanes := 4
	if len(v) < lanes {
		result := v[0]
		for i := 1; i < len(v); i++ {
			if v[i] < result {
				result = v[i]
			}
		}
		return result
	}
	minVec := asm.LoadFloat32x4Slice(v)
	var i int
	for i = lanes; i+lanes*2 <= len(v); i += lanes * 2 {
		va := asm.LoadFloat32x4((*[4]float32)(unsafe.Pointer(&v[i])))
		minVec = minVec.Min(va)
		va1 := asm.LoadFloat32x4((*[4]float32)(unsafe.Pointer(&v[i+4])))
		minVec = minVec.Min(va1)
	}
	result := minVec.ReduceMin()
	for ; i < len(v); i++ {
		if v[i] < result {
			result = v[i]
		}
	}
	return result
}

func BaseMin_neon_Float64(v []float64) float64 {
	if len(v) == 0 {
		panic("vec: Min called on empty slice")
	}
	lanes := 2
	if len(v) < lanes {
		result := v[0]
		for i := 1; i < len(v); i++ {
			if v[i] < result {
				result = v[i]
			}
		}
		return result
	}
	minVec := asm.LoadFloat64x2Slice(v)
	var i int
	for i = lanes; i+lanes*2 <= len(v); i += lanes * 2 {
		va := asm.LoadFloat64x2((*[2]float64)(unsafe.Pointer(&v[i])))
		minVec = minVec.Min(va)
		va1 := asm.LoadFloat64x2((*[2]float64)(unsafe.Pointer(&v[i+2])))
		minVec = minVec.Min(va1)
	}
	result := minVec.ReduceMin()
	for ; i < len(v); i++ {
		if v[i] < result {
			result = v[i]
		}
	}
	return result
}

func BaseMax_neon(v []float32) float32 {
	if len(v) == 0 {
		panic("vec: Max called on empty slice")
	}
	lanes := 4
	if len(v) < lanes {
		result := v[0]
		for i := 1; i < len(v); i++ {
			if v[i] > result {
				result = v[i]
			}
		}
		return result
	}
	maxVec := asm.LoadFloat32x4Slice(v)
	var i int
	for i = lanes; i+lanes*2 <= len(v); i += lanes * 2 {
		va := asm.LoadFloat32x4((*[4]float32)(unsafe.Pointer(&v[i])))
		maxVec = maxVec.Max(va)
		va1 := asm.LoadFloat32x4((*[4]float32)(unsafe.Pointer(&v[i+4])))
		maxVec = maxVec.Max(va1)
	}
	result := maxVec.ReduceMax()
	for ; i < len(v); i++ {
		if v[i] > result {
			result = v[i]
		}
	}
	return result
}

func BaseMax_neon_Float64(v []float64) float64 {
	if len(v) == 0 {
		panic("vec: Max called on empty slice")
	}
	lanes := 2
	if len(v) < lanes {
		result := v[0]
		for i := 1; i < len(v); i++ {
			if v[i] > result {
				result = v[i]
			}
		}
		return result
	}
	maxVec := asm.LoadFloat64x2Slice(v)
	var i int
	for i = lanes; i+lanes*2 <= len(v); i += lanes * 2 {
		va := asm.LoadFloat64x2((*[2]float64)(unsafe.Pointer(&v[i])))
		maxVec = maxVec.Max(va)
		va1 := asm.LoadFloat64x2((*[2]float64)(unsafe.Pointer(&v[i+2])))
		maxVec = maxVec.Max(va1)
	}
	result := maxVec.ReduceMax()
	for ; i < len(v); i++ {
		if v[i] > result {
			result = v[i]
		}
	}
	return result
}

func BaseMax_neon_Int32(v []int32) int32 {
	if len(v) == 0 {
		panic("vec: Max called on empty slice")
	}
	lanes := 4
	if len(v) < lanes {
		result := v[0]
		for i := 1; i < len(v); i++ {
			if v[i] > result {
				result = v[i]
			}
		}
		return result
	}
	maxVec := asm.LoadInt32x4Slice(v)
	var i int
	for i = lanes; i+lanes*2 <= len(v); i += lanes * 2 {
		va := asm.LoadInt32x4((*[4]int32)(unsafe.Pointer(&v[i])))
		maxVec = maxVec.Max(va)
		va1 := asm.LoadInt32x4((*[4]int32)(unsafe.Pointer(&v[i+4])))
		maxVec = maxVec.Max(va1)
	}
	result := maxVec.ReduceMax()
	for ; i < len(v); i++ {
		if v[i] > result {
			result = v[i]
		}
	}
	return result
}

func BaseMax_neon_Int64(v []int64) int64 {
	if len(v) == 0 {
		panic("vec: Max called on empty slice")
	}
	lanes := 2
	if len(v) < lanes {
		result := v[0]
		for i := 1; i < len(v); i++ {
			if v[i] > result {
				result = v[i]
			}
		}
		return result
	}
	maxVec := asm.LoadInt64x2Slice(v)
	var i int
	for i = lanes; i+lanes*2 <= len(v); i += lanes * 2 {
		va := asm.LoadInt64x2((*[2]int64)(unsafe.Pointer(&v[i])))
		maxVec = maxVec.Max(va)
		va1 := asm.LoadInt64x2((*[2]int64)(unsafe.Pointer(&v[i+2])))
		maxVec = maxVec.Max(va1)
	}
	result := maxVec.ReduceMax()
	for ; i < len(v); i++ {
		if v[i] > result {
			result = v[i]
		}
	}
	return result
}

func BaseMax_neon_Uint32(v []uint32) uint32 {
	if len(v) == 0 {
		panic("vec: Max called on empty slice")
	}
	lanes := 4
	if len(v) < lanes {
		result := v[0]
		for i := 1; i < len(v); i++ {
			if v[i] > result {
				result = v[i]
			}
		}
		return result
	}
	maxVec := asm.LoadUint32x4Slice(v)
	var i int
	for i = lanes; i+lanes*2 <= len(v); i += lanes * 2 {
		va := asm.LoadUint32x4((*[4]uint32)(unsafe.Pointer(&v[i])))
		maxVec = maxVec.Max(va)
		va1 := asm.LoadUint32x4((*[4]uint32)(unsafe.Pointer(&v[i+4])))
		maxVec = maxVec.Max(va1)
	}
	result := maxVec.ReduceMax()
	for ; i < len(v); i++ {
		if v[i] > result {
			result = v[i]
		}
	}
	return result
}

func BaseMax_neon_Uint64(v []uint64) uint64 {
	if len(v) == 0 {
		panic("vec: Max called on empty slice")
	}
	lanes := 2
	if len(v) < lanes {
		result := v[0]
		for i := 1; i < len(v); i++ {
			if v[i] > result {
				result = v[i]
			}
		}
		return result
	}
	maxVec := asm.LoadUint64x2Slice(v)
	var i int
	for i = lanes; i+lanes*2 <= len(v); i += lanes * 2 {
		va := asm.LoadUint64x2((*[2]uint64)(unsafe.Pointer(&v[i])))
		maxVec = maxVec.Max(va)
		va1 := asm.LoadUint64x2((*[2]uint64)(unsafe.Pointer(&v[i+2])))
		maxVec = maxVec.Max(va1)
	}
	result := maxVec.ReduceMax()
	for ; i < len(v); i++ {
		if v[i] > result {
			result = v[i]
		}
	}
	return result
}

func BaseMinMax_neon_Float16(v []hwy.Float16) (minVal hwy.Float16, maxVal hwy.Float16) {
	if len(v) == 0 {
		panic("vec: MinMax called on empty slice")
	}
	lanes := 8
	if len(v) < lanes {
		minVal = v[0]
		maxVal = v[0]
		for i := 1; i < len(v); i++ {
			if v[i].Float32() < minVal.Float32() {
				minVal = v[i]
			}
			if v[i].Float32() > maxVal.Float32() {
				maxVal = v[i]
			}
		}
		return minVal, maxVal
	}
	minVec := asm.LoadFloat16x8Ptr(unsafe.Pointer(&v[0]))
	maxVec := minVec
	var i int
	for i = lanes; i+lanes*2 <= len(v); i += lanes * 2 {
		va := asm.LoadFloat16x8Ptr(unsafe.Pointer(&v[i:][0]))
		minVec = minVec.Min(va)
		maxVec = maxVec.Max(va)
		va1 := asm.LoadFloat16x8Ptr(unsafe.Pointer(&v[i+8:][0]))
		minVec = minVec.Min(va1)
		maxVec = maxVec.Max(va1)
	}
	minVal = hwy.Float32ToFloat16(minVec.ReduceMin())
	maxVal = hwy.Float32ToFloat16(maxVec.ReduceMax())
	for ; i < len(v); i++ {
		if v[i].Float32() < minVal.Float32() {
			minVal = v[i]
		}
		if v[i].Float32() > maxVal.Float32() {
			maxVal = v[i]
		}
	}
	return minVal, maxVal
}

func BaseMinMax_neon_BFloat16(v []hwy.BFloat16) (minVal hwy.BFloat16, maxVal hwy.BFloat16) {
	if len(v) == 0 {
		panic("vec: MinMax called on empty slice")
	}
	lanes := 8
	if len(v) < lanes {
		minVal = v[0]
		maxVal = v[0]
		for i := 1; i < len(v); i++ {
			if v[i].Float32() < minVal.Float32() {
				minVal = v[i]
			}
			if v[i].Float32() > maxVal.Float32() {
				maxVal = v[i]
			}
		}
		return minVal, maxVal
	}
	minVec := asm.LoadBFloat16x8Ptr(unsafe.Pointer(&v[0]))
	maxVec := minVec
	var i int
	for i = lanes; i+lanes*2 <= len(v); i += lanes * 2 {
		va := asm.LoadBFloat16x8Ptr(unsafe.Pointer(&v[i:][0]))
		minVec = minVec.Min(va)
		maxVec = maxVec.Max(va)
		va1 := asm.LoadBFloat16x8Ptr(unsafe.Pointer(&v[i+8:][0]))
		minVec = minVec.Min(va1)
		maxVec = maxVec.Max(va1)
	}
	minVal = hwy.Float32ToBFloat16(minVec.ReduceMin())
	maxVal = hwy.Float32ToBFloat16(maxVec.ReduceMax())
	for ; i < len(v); i++ {
		if v[i].Float32() < minVal.Float32() {
			minVal = v[i]
		}
		if v[i].Float32() > maxVal.Float32() {
			maxVal = v[i]
		}
	}
	return minVal, maxVal
}

func BaseMinMax_neon(v []float32) (minVal float32, maxVal float32) {
	if len(v) == 0 {
		panic("vec: MinMax called on empty slice")
	}
	lanes := 4
	if len(v) < lanes {
		minVal = v[0]
		maxVal = v[0]
		for i := 1; i < len(v); i++ {
			if v[i] < minVal {
				minVal = v[i]
			}
			if v[i] > maxVal {
				maxVal = v[i]
			}
		}
		return minVal, maxVal
	}
	minVec := asm.LoadFloat32x4Slice(v)
	maxVec := minVec
	var i int
	for i = lanes; i+lanes*2 <= len(v); i += lanes * 2 {
		va := asm.LoadFloat32x4((*[4]float32)(unsafe.Pointer(&v[i])))
		minVec = minVec.Min(va)
		maxVec = maxVec.Max(va)
		va1 := asm.LoadFloat32x4((*[4]float32)(unsafe.Pointer(&v[i+4])))
		minVec = minVec.Min(va1)
		maxVec = maxVec.Max(va1)
	}
	minVal = minVec.ReduceMin()
	maxVal = maxVec.ReduceMax()
	for ; i < len(v); i++ {
		if v[i] < minVal {
			minVal = v[i]
		}
		if v[i] > maxVal {
			maxVal = v[i]
		}
	}
	return minVal, maxVal
}

func BaseMinMax_neon_Float64(v []float64) (minVal float64, maxVal float64) {
	if len(v) == 0 {
		panic("vec: MinMax called on empty slice")
	}
	lanes := 2
	if len(v) < lanes {
		minVal = v[0]
		maxVal = v[0]
		for i := 1; i < len(v); i++ {
			if v[i] < minVal {
				minVal = v[i]
			}
			if v[i] > maxVal {
				maxVal = v[i]
			}
		}
		return minVal, maxVal
	}
	minVec := asm.LoadFloat64x2Slice(v)
	maxVec := minVec
	var i int
	for i = lanes; i+lanes*2 <= len(v); i += lanes * 2 {
		va := asm.LoadFloat64x2((*[2]float64)(unsafe.Pointer(&v[i])))
		minVec = minVec.Min(va)
		maxVec = maxVec.Max(va)
		va1 := asm.LoadFloat64x2((*[2]float64)(unsafe.Pointer(&v[i+2])))
		minVec = minVec.Min(va1)
		maxVec = maxVec.Max(va1)
	}
	minVal = minVec.ReduceMin()
	maxVal = maxVec.ReduceMax()
	for ; i < len(v); i++ {
		if v[i] < minVal {
			minVal = v[i]
		}
		if v[i] > maxVal {
			maxVal = v[i]
		}
	}
	return minVal, maxVal
}
