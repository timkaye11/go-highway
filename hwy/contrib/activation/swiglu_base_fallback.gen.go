// Code generated by github.com/ajroetker/go-highway/cmd/hwygen. DO NOT EDIT.

package activation

import (
	stdmath "math"

	"github.com/ajroetker/go-highway/hwy"
	"github.com/ajroetker/go-highway/hwy/contrib/math"
)

func BaseSwiGLU_fallback_Float16(gate []hwy.Float16, up []hwy.Float16, output []hwy.Float16) {
	size := min(len(gate), min(len(up), len(output)))
	if size == 0 {
		return
	}
	lanes := hwy.MaxLanes[hwy.Float16]()
	ii := 0
	for ; ii+lanes <= size; ii += lanes {
		g := hwy.Load(gate[ii:])
		u := hwy.Load(up[ii:])
		sigG := math.BaseSigmoidVec_fallback_Float16(g)
		siluG := hwy.Mul(g, sigG)
		result := hwy.Mul(siluG, u)
		hwy.Store(result, output[ii:])
	}
	for i := ii; i < size; i++ {
		g := float64(gate[i].Float32())
		sig := 1.0 / (1.0 + stdmath.Exp(-g))
		output[i] = hwy.Float32ToFloat16(float32(g * sig * float64(up[i].Float32())))
	}
}

func BaseSwiGLU_fallback_BFloat16(gate []hwy.BFloat16, up []hwy.BFloat16, output []hwy.BFloat16) {
	size := min(len(gate), min(len(up), len(output)))
	if size == 0 {
		return
	}
	lanes := hwy.MaxLanes[hwy.BFloat16]()
	ii := 0
	for ; ii+lanes <= size; ii += lanes {
		g := hwy.Load(gate[ii:])
		u := hwy.Load(up[ii:])
		sigG := math.BaseSigmoidVec_fallback_BFloat16(g)
		siluG := hwy.Mul(g, sigG)
		result := hwy.Mul(siluG, u)
		hwy.Store(result, output[ii:])
	}
	for i := ii; i < size; i++ {
		g := float64(gate[i].Float32())
		sig := 1.0 / (1.0 + stdmath.Exp(-g))
		output[i] = hwy.Float32ToBFloat16(float32(g * sig * float64(up[i].Float32())))
	}
}

func BaseSwiGLU_fallback(gate []float32, up []float32, output []float32) {
	size := min(len(gate), min(len(up), len(output)))
	if size == 0 {
		return
	}
	lanes := hwy.MaxLanes[float32]()
	ii := 0
	for ; ii+lanes <= size; ii += lanes {
		g := hwy.Load(gate[ii:])
		u := hwy.Load(up[ii:])
		sigG := math.BaseSigmoidVec_fallback(g)
		siluG := hwy.Mul(g, sigG)
		result := hwy.Mul(siluG, u)
		hwy.Store(result, output[ii:])
	}
	for i := ii; i < size; i++ {
		g := float64(gate[i])
		sig := 1.0 / (1.0 + stdmath.Exp(-g))
		output[i] = float32(g * sig * float64(up[i]))
	}
}

func BaseSwiGLU_fallback_Float64(gate []float64, up []float64, output []float64) {
	size := min(len(gate), min(len(up), len(output)))
	if size == 0 {
		return
	}
	lanes := hwy.MaxLanes[float64]()
	ii := 0
	for ; ii+lanes <= size; ii += lanes {
		g := hwy.Load(gate[ii:])
		u := hwy.Load(up[ii:])
		sigG := math.BaseSigmoidVec_fallback_Float64(g)
		siluG := hwy.Mul(g, sigG)
		result := hwy.Mul(siluG, u)
		hwy.Store(result, output[ii:])
	}
	for i := ii; i < size; i++ {
		g := float64(gate[i])
		sig := 1.0 / (1.0 + stdmath.Exp(-g))
		output[i] = float64(g * sig * float64(up[i]))
	}
}
