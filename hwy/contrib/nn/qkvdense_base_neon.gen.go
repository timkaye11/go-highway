// Code generated by github.com/ajroetker/go-highway/cmd/hwygen. DO NOT EDIT.

//go:build arm64

package nn

import (
	"unsafe"

	"github.com/ajroetker/go-highway/hwy"
	"github.com/ajroetker/go-highway/hwy/asm"
)

func BaseQKVDense_neon_Float16(x []hwy.Float16, wQKV []hwy.Float16, biasQ []hwy.Float16, biasK []hwy.Float16, biasV []hwy.Float16, q []hwy.Float16, k []hwy.Float16, v []hwy.Float16, batchSize int, inFeatures int, qDim int, kvDim int) {
	totalOut := qDim + 2*kvDim
	if len(x) < batchSize*inFeatures {
		panic("qkvdense: x slice too short")
	}
	if len(wQKV) < totalOut*inFeatures {
		panic("qkvdense: wQKV slice too short")
	}
	if len(q) < batchSize*qDim {
		panic("qkvdense: q slice too short")
	}
	if len(k) < batchSize*kvDim {
		panic("qkvdense: k slice too short")
	}
	if len(v) < batchSize*kvDim {
		panic("qkvdense: v slice too short")
	}
	lanes := 8
	var i int
	for i = 0; i+3 < batchSize; i += 4 {
		xRow0 := i * inFeatures
		xRow1 := (i + 1) * inFeatures
		xRow2 := (i + 2) * inFeatures
		xRow3 := (i + 3) * inFeatures
		for j := 0; j < totalOut; j++ {
			wRow := j * inFeatures
			acc0 := asm.ZeroFloat16x8()
			acc1 := asm.ZeroFloat16x8()
			acc2 := asm.ZeroFloat16x8()
			acc3 := asm.ZeroFloat16x8()
			var p int
			for p = 0; p+lanes <= inFeatures; p += lanes {
				vW := asm.LoadFloat16x8Ptr(unsafe.Pointer(&wQKV[wRow+p:][0]))
				vX0 := asm.LoadFloat16x8Ptr(unsafe.Pointer(&x[xRow0+p:][0]))
				vX1 := asm.LoadFloat16x8Ptr(unsafe.Pointer(&x[xRow1+p:][0]))
				vX2 := asm.LoadFloat16x8Ptr(unsafe.Pointer(&x[xRow2+p:][0]))
				vX3 := asm.LoadFloat16x8Ptr(unsafe.Pointer(&x[xRow3+p:][0]))
				vX0.MulAddAcc(vW, &acc0)
				vX1.MulAddAcc(vW, &acc1)
				vX2.MulAddAcc(vW, &acc2)
				vX3.MulAddAcc(vW, &acc3)
			}
			sum0 := acc0.ReduceSum()
			sum1 := acc1.ReduceSum()
			sum2 := acc2.ReduceSum()
			sum3 := acc3.ReduceSum()
			for ; p < inFeatures; p++ {
				sum0 += x[xRow0+p].Float32() * wQKV[wRow+p].Float32()
				sum1 += x[xRow1+p].Float32() * wQKV[wRow+p].Float32()
				sum2 += x[xRow2+p].Float32() * wQKV[wRow+p].Float32()
				sum3 += x[xRow3+p].Float32() * wQKV[wRow+p].Float32()
			}
			if j < qDim {
				if biasQ != nil {
					b := biasQ[j]
					sum0 += b.Float32()
					sum1 += b.Float32()
					sum2 += b.Float32()
					sum3 += b.Float32()
				}
				q[i*qDim+j] = hwy.Float32ToFloat16(sum0)
				q[(i+1)*qDim+j] = hwy.Float32ToFloat16(sum1)
				q[(i+2)*qDim+j] = hwy.Float32ToFloat16(sum2)
				q[(i+3)*qDim+j] = hwy.Float32ToFloat16(sum3)
			} else if j < qDim+kvDim {
				kj := j - qDim
				if biasK != nil {
					b := biasK[kj]
					sum0 += b.Float32()
					sum1 += b.Float32()
					sum2 += b.Float32()
					sum3 += b.Float32()
				}
				k[i*kvDim+kj] = hwy.Float32ToFloat16(sum0)
				k[(i+1)*kvDim+kj] = hwy.Float32ToFloat16(sum1)
				k[(i+2)*kvDim+kj] = hwy.Float32ToFloat16(sum2)
				k[(i+3)*kvDim+kj] = hwy.Float32ToFloat16(sum3)
			} else {
				vj := j - qDim - kvDim
				if biasV != nil {
					b := biasV[vj]
					sum0 += b.Float32()
					sum1 += b.Float32()
					sum2 += b.Float32()
					sum3 += b.Float32()
				}
				v[i*kvDim+vj] = hwy.Float32ToFloat16(sum0)
				v[(i+1)*kvDim+vj] = hwy.Float32ToFloat16(sum1)
				v[(i+2)*kvDim+vj] = hwy.Float32ToFloat16(sum2)
				v[(i+3)*kvDim+vj] = hwy.Float32ToFloat16(sum3)
			}
		}
	}
	for ; i < batchSize; i++ {
		xRow := i * inFeatures
		for j := 0; j < totalOut; j++ {
			wRow := j * inFeatures
			acc := asm.ZeroFloat16x8()
			var p int
			for p = 0; p+lanes <= inFeatures; p += lanes {
				vX := asm.LoadFloat16x8Ptr(unsafe.Pointer(&x[xRow+p:][0]))
				vW := asm.LoadFloat16x8Ptr(unsafe.Pointer(&wQKV[wRow+p:][0]))
				vX.MulAddAcc(vW, &acc)
			}
			sum := acc.ReduceSum()
			for ; p < inFeatures; p++ {
				sum += x[xRow+p].Float32() * wQKV[wRow+p].Float32()
			}
			if j < qDim {
				if biasQ != nil {
					sum += biasQ[j].Float32()
				}
				q[i*qDim+j] = hwy.Float32ToFloat16(sum)
			} else if j < qDim+kvDim {
				kj := j - qDim
				if biasK != nil {
					sum += biasK[kj].Float32()
				}
				k[i*kvDim+kj] = hwy.Float32ToFloat16(sum)
			} else {
				vj := j - qDim - kvDim
				if biasV != nil {
					sum += biasV[vj].Float32()
				}
				v[i*kvDim+vj] = hwy.Float32ToFloat16(sum)
			}
		}
	}
}

func BaseQKVDense_neon_BFloat16(x []hwy.BFloat16, wQKV []hwy.BFloat16, biasQ []hwy.BFloat16, biasK []hwy.BFloat16, biasV []hwy.BFloat16, q []hwy.BFloat16, k []hwy.BFloat16, v []hwy.BFloat16, batchSize int, inFeatures int, qDim int, kvDim int) {
	totalOut := qDim + 2*kvDim
	if len(x) < batchSize*inFeatures {
		panic("qkvdense: x slice too short")
	}
	if len(wQKV) < totalOut*inFeatures {
		panic("qkvdense: wQKV slice too short")
	}
	if len(q) < batchSize*qDim {
		panic("qkvdense: q slice too short")
	}
	if len(k) < batchSize*kvDim {
		panic("qkvdense: k slice too short")
	}
	if len(v) < batchSize*kvDim {
		panic("qkvdense: v slice too short")
	}
	lanes := 8
	var i int
	for i = 0; i+3 < batchSize; i += 4 {
		xRow0 := i * inFeatures
		xRow1 := (i + 1) * inFeatures
		xRow2 := (i + 2) * inFeatures
		xRow3 := (i + 3) * inFeatures
		for j := 0; j < totalOut; j++ {
			wRow := j * inFeatures
			acc0 := asm.ZeroBFloat16x8()
			acc1 := asm.ZeroBFloat16x8()
			acc2 := asm.ZeroBFloat16x8()
			acc3 := asm.ZeroBFloat16x8()
			var p int
			for p = 0; p+lanes <= inFeatures; p += lanes {
				vW := asm.LoadBFloat16x8Ptr(unsafe.Pointer(&wQKV[wRow+p:][0]))
				vX0 := asm.LoadBFloat16x8Ptr(unsafe.Pointer(&x[xRow0+p:][0]))
				vX1 := asm.LoadBFloat16x8Ptr(unsafe.Pointer(&x[xRow1+p:][0]))
				vX2 := asm.LoadBFloat16x8Ptr(unsafe.Pointer(&x[xRow2+p:][0]))
				vX3 := asm.LoadBFloat16x8Ptr(unsafe.Pointer(&x[xRow3+p:][0]))
				vX0.MulAddAcc(vW, &acc0)
				vX1.MulAddAcc(vW, &acc1)
				vX2.MulAddAcc(vW, &acc2)
				vX3.MulAddAcc(vW, &acc3)
			}
			sum0 := acc0.ReduceSum()
			sum1 := acc1.ReduceSum()
			sum2 := acc2.ReduceSum()
			sum3 := acc3.ReduceSum()
			for ; p < inFeatures; p++ {
				sum0 += x[xRow0+p].Float32() * wQKV[wRow+p].Float32()
				sum1 += x[xRow1+p].Float32() * wQKV[wRow+p].Float32()
				sum2 += x[xRow2+p].Float32() * wQKV[wRow+p].Float32()
				sum3 += x[xRow3+p].Float32() * wQKV[wRow+p].Float32()
			}
			if j < qDim {
				if biasQ != nil {
					b := biasQ[j]
					sum0 += b.Float32()
					sum1 += b.Float32()
					sum2 += b.Float32()
					sum3 += b.Float32()
				}
				q[i*qDim+j] = hwy.Float32ToBFloat16(sum0)
				q[(i+1)*qDim+j] = hwy.Float32ToBFloat16(sum1)
				q[(i+2)*qDim+j] = hwy.Float32ToBFloat16(sum2)
				q[(i+3)*qDim+j] = hwy.Float32ToBFloat16(sum3)
			} else if j < qDim+kvDim {
				kj := j - qDim
				if biasK != nil {
					b := biasK[kj]
					sum0 += b.Float32()
					sum1 += b.Float32()
					sum2 += b.Float32()
					sum3 += b.Float32()
				}
				k[i*kvDim+kj] = hwy.Float32ToBFloat16(sum0)
				k[(i+1)*kvDim+kj] = hwy.Float32ToBFloat16(sum1)
				k[(i+2)*kvDim+kj] = hwy.Float32ToBFloat16(sum2)
				k[(i+3)*kvDim+kj] = hwy.Float32ToBFloat16(sum3)
			} else {
				vj := j - qDim - kvDim
				if biasV != nil {
					b := biasV[vj]
					sum0 += b.Float32()
					sum1 += b.Float32()
					sum2 += b.Float32()
					sum3 += b.Float32()
				}
				v[i*kvDim+vj] = hwy.Float32ToBFloat16(sum0)
				v[(i+1)*kvDim+vj] = hwy.Float32ToBFloat16(sum1)
				v[(i+2)*kvDim+vj] = hwy.Float32ToBFloat16(sum2)
				v[(i+3)*kvDim+vj] = hwy.Float32ToBFloat16(sum3)
			}
		}
	}
	for ; i < batchSize; i++ {
		xRow := i * inFeatures
		for j := 0; j < totalOut; j++ {
			wRow := j * inFeatures
			acc := asm.ZeroBFloat16x8()
			var p int
			for p = 0; p+lanes <= inFeatures; p += lanes {
				vX := asm.LoadBFloat16x8Ptr(unsafe.Pointer(&x[xRow+p:][0]))
				vW := asm.LoadBFloat16x8Ptr(unsafe.Pointer(&wQKV[wRow+p:][0]))
				vX.MulAddAcc(vW, &acc)
			}
			sum := acc.ReduceSum()
			for ; p < inFeatures; p++ {
				sum += x[xRow+p].Float32() * wQKV[wRow+p].Float32()
			}
			if j < qDim {
				if biasQ != nil {
					sum += biasQ[j].Float32()
				}
				q[i*qDim+j] = hwy.Float32ToBFloat16(sum)
			} else if j < qDim+kvDim {
				kj := j - qDim
				if biasK != nil {
					sum += biasK[kj].Float32()
				}
				k[i*kvDim+kj] = hwy.Float32ToBFloat16(sum)
			} else {
				vj := j - qDim - kvDim
				if biasV != nil {
					sum += biasV[vj].Float32()
				}
				v[i*kvDim+vj] = hwy.Float32ToBFloat16(sum)
			}
		}
	}
}

func BaseQKVDense_neon(x []float32, wQKV []float32, biasQ []float32, biasK []float32, biasV []float32, q []float32, k []float32, v []float32, batchSize int, inFeatures int, qDim int, kvDim int) {
	totalOut := qDim + 2*kvDim
	if len(x) < batchSize*inFeatures {
		panic("qkvdense: x slice too short")
	}
	if len(wQKV) < totalOut*inFeatures {
		panic("qkvdense: wQKV slice too short")
	}
	if len(q) < batchSize*qDim {
		panic("qkvdense: q slice too short")
	}
	if len(k) < batchSize*kvDim {
		panic("qkvdense: k slice too short")
	}
	if len(v) < batchSize*kvDim {
		panic("qkvdense: v slice too short")
	}
	lanes := 4
	var i int
	for i = 0; i+3 < batchSize; i += 4 {
		xRow0 := i * inFeatures
		xRow1 := (i + 1) * inFeatures
		xRow2 := (i + 2) * inFeatures
		xRow3 := (i + 3) * inFeatures
		for j := 0; j < totalOut; j++ {
			wRow := j * inFeatures
			acc0 := asm.ZeroFloat32x4()
			acc1 := asm.ZeroFloat32x4()
			acc2 := asm.ZeroFloat32x4()
			acc3 := asm.ZeroFloat32x4()
			var p int
			for p = 0; p+lanes <= inFeatures; p += lanes {
				vW := asm.LoadFloat32x4Slice(wQKV[wRow+p:])
				vX0 := asm.LoadFloat32x4Slice(x[xRow0+p:])
				vX1 := asm.LoadFloat32x4Slice(x[xRow1+p:])
				vX2 := asm.LoadFloat32x4Slice(x[xRow2+p:])
				vX3 := asm.LoadFloat32x4Slice(x[xRow3+p:])
				vX0.MulAddAcc(vW, &acc0)
				vX1.MulAddAcc(vW, &acc1)
				vX2.MulAddAcc(vW, &acc2)
				vX3.MulAddAcc(vW, &acc3)
			}
			sum0 := acc0.ReduceSum()
			sum1 := acc1.ReduceSum()
			sum2 := acc2.ReduceSum()
			sum3 := acc3.ReduceSum()
			for ; p < inFeatures; p++ {
				sum0 += x[xRow0+p] * wQKV[wRow+p]
				sum1 += x[xRow1+p] * wQKV[wRow+p]
				sum2 += x[xRow2+p] * wQKV[wRow+p]
				sum3 += x[xRow3+p] * wQKV[wRow+p]
			}
			if j < qDim {
				if biasQ != nil {
					b := biasQ[j]
					sum0 += b
					sum1 += b
					sum2 += b
					sum3 += b
				}
				q[i*qDim+j] = sum0
				q[(i+1)*qDim+j] = sum1
				q[(i+2)*qDim+j] = sum2
				q[(i+3)*qDim+j] = sum3
			} else if j < qDim+kvDim {
				kj := j - qDim
				if biasK != nil {
					b := biasK[kj]
					sum0 += b
					sum1 += b
					sum2 += b
					sum3 += b
				}
				k[i*kvDim+kj] = sum0
				k[(i+1)*kvDim+kj] = sum1
				k[(i+2)*kvDim+kj] = sum2
				k[(i+3)*kvDim+kj] = sum3
			} else {
				vj := j - qDim - kvDim
				if biasV != nil {
					b := biasV[vj]
					sum0 += b
					sum1 += b
					sum2 += b
					sum3 += b
				}
				v[i*kvDim+vj] = sum0
				v[(i+1)*kvDim+vj] = sum1
				v[(i+2)*kvDim+vj] = sum2
				v[(i+3)*kvDim+vj] = sum3
			}
		}
	}
	for ; i < batchSize; i++ {
		xRow := i * inFeatures
		for j := 0; j < totalOut; j++ {
			wRow := j * inFeatures
			acc := asm.ZeroFloat32x4()
			var p int
			for p = 0; p+lanes <= inFeatures; p += lanes {
				vX := asm.LoadFloat32x4Slice(x[xRow+p:])
				vW := asm.LoadFloat32x4Slice(wQKV[wRow+p:])
				vX.MulAddAcc(vW, &acc)
			}
			sum := acc.ReduceSum()
			for ; p < inFeatures; p++ {
				sum += x[xRow+p] * wQKV[wRow+p]
			}
			if j < qDim {
				if biasQ != nil {
					sum += biasQ[j]
				}
				q[i*qDim+j] = sum
			} else if j < qDim+kvDim {
				kj := j - qDim
				if biasK != nil {
					sum += biasK[kj]
				}
				k[i*kvDim+kj] = sum
			} else {
				vj := j - qDim - kvDim
				if biasV != nil {
					sum += biasV[vj]
				}
				v[i*kvDim+vj] = sum
			}
		}
	}
}

func BaseQKVDense_neon_Float64(x []float64, wQKV []float64, biasQ []float64, biasK []float64, biasV []float64, q []float64, k []float64, v []float64, batchSize int, inFeatures int, qDim int, kvDim int) {
	totalOut := qDim + 2*kvDim
	if len(x) < batchSize*inFeatures {
		panic("qkvdense: x slice too short")
	}
	if len(wQKV) < totalOut*inFeatures {
		panic("qkvdense: wQKV slice too short")
	}
	if len(q) < batchSize*qDim {
		panic("qkvdense: q slice too short")
	}
	if len(k) < batchSize*kvDim {
		panic("qkvdense: k slice too short")
	}
	if len(v) < batchSize*kvDim {
		panic("qkvdense: v slice too short")
	}
	lanes := 2
	var i int
	for i = 0; i+3 < batchSize; i += 4 {
		xRow0 := i * inFeatures
		xRow1 := (i + 1) * inFeatures
		xRow2 := (i + 2) * inFeatures
		xRow3 := (i + 3) * inFeatures
		for j := 0; j < totalOut; j++ {
			wRow := j * inFeatures
			acc0 := asm.ZeroFloat64x2()
			acc1 := asm.ZeroFloat64x2()
			acc2 := asm.ZeroFloat64x2()
			acc3 := asm.ZeroFloat64x2()
			var p int
			for p = 0; p+lanes <= inFeatures; p += lanes {
				vW := asm.LoadFloat64x2Slice(wQKV[wRow+p:])
				vX0 := asm.LoadFloat64x2Slice(x[xRow0+p:])
				vX1 := asm.LoadFloat64x2Slice(x[xRow1+p:])
				vX2 := asm.LoadFloat64x2Slice(x[xRow2+p:])
				vX3 := asm.LoadFloat64x2Slice(x[xRow3+p:])
				vX0.MulAddAcc(vW, &acc0)
				vX1.MulAddAcc(vW, &acc1)
				vX2.MulAddAcc(vW, &acc2)
				vX3.MulAddAcc(vW, &acc3)
			}
			sum0 := acc0.ReduceSum()
			sum1 := acc1.ReduceSum()
			sum2 := acc2.ReduceSum()
			sum3 := acc3.ReduceSum()
			for ; p < inFeatures; p++ {
				sum0 += x[xRow0+p] * wQKV[wRow+p]
				sum1 += x[xRow1+p] * wQKV[wRow+p]
				sum2 += x[xRow2+p] * wQKV[wRow+p]
				sum3 += x[xRow3+p] * wQKV[wRow+p]
			}
			if j < qDim {
				if biasQ != nil {
					b := biasQ[j]
					sum0 += b
					sum1 += b
					sum2 += b
					sum3 += b
				}
				q[i*qDim+j] = sum0
				q[(i+1)*qDim+j] = sum1
				q[(i+2)*qDim+j] = sum2
				q[(i+3)*qDim+j] = sum3
			} else if j < qDim+kvDim {
				kj := j - qDim
				if biasK != nil {
					b := biasK[kj]
					sum0 += b
					sum1 += b
					sum2 += b
					sum3 += b
				}
				k[i*kvDim+kj] = sum0
				k[(i+1)*kvDim+kj] = sum1
				k[(i+2)*kvDim+kj] = sum2
				k[(i+3)*kvDim+kj] = sum3
			} else {
				vj := j - qDim - kvDim
				if biasV != nil {
					b := biasV[vj]
					sum0 += b
					sum1 += b
					sum2 += b
					sum3 += b
				}
				v[i*kvDim+vj] = sum0
				v[(i+1)*kvDim+vj] = sum1
				v[(i+2)*kvDim+vj] = sum2
				v[(i+3)*kvDim+vj] = sum3
			}
		}
	}
	for ; i < batchSize; i++ {
		xRow := i * inFeatures
		for j := 0; j < totalOut; j++ {
			wRow := j * inFeatures
			acc := asm.ZeroFloat64x2()
			var p int
			for p = 0; p+lanes <= inFeatures; p += lanes {
				vX := asm.LoadFloat64x2Slice(x[xRow+p:])
				vW := asm.LoadFloat64x2Slice(wQKV[wRow+p:])
				vX.MulAddAcc(vW, &acc)
			}
			sum := acc.ReduceSum()
			for ; p < inFeatures; p++ {
				sum += x[xRow+p] * wQKV[wRow+p]
			}
			if j < qDim {
				if biasQ != nil {
					sum += biasQ[j]
				}
				q[i*qDim+j] = sum
			} else if j < qDim+kvDim {
				kj := j - qDim
				if biasK != nil {
					sum += biasK[kj]
				}
				k[i*kvDim+kj] = sum
			} else {
				vj := j - qDim - kvDim
				if biasV != nil {
					sum += biasV[vj]
				}
				v[i*kvDim+vj] = sum
			}
		}
	}
}
