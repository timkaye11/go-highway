// Code generated by github.com/ajroetker/go-highway/cmd/hwygen. DO NOT EDIT.

//go:build amd64 && goexperiment.simd

package nn

import (
	stdmath "math"
	"simd/archsimd"
	"unsafe"

	"github.com/ajroetker/go-highway/hwy"
	"github.com/ajroetker/go-highway/hwy/asm"
)

func BaseLayerNorm_avx2_Float16(input []hwy.Float16, output []hwy.Float16, normSize int, gamma []hwy.Float16, beta []hwy.Float16, epsilon hwy.Float16) {
	size := min(len(input), len(output))
	if size == 0 || normSize <= 0 {
		return
	}
	numGroups := size / normSize
	invN := float32(1.0) / float32(normSize)
	lanes := 8
	for g := 0; g < numGroups; g++ {
		off := g * normSize
		sumAcc := asm.ZeroFloat16x8AVX2()
		ii := 0
		for ; ii+lanes <= normSize; ii += lanes {
			x := asm.LoadFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(input[off+ii:]))), len(input[off+ii:])))
			sumAcc = sumAcc.Add(x)
		}
		mean := sumAcc.ReduceSum()
		for i := ii; i < normSize; i++ {
			mean += input[off+i].Float32()
		}
		mean *= invN
		vMean := asm.BroadcastFloat16x8AVX2(uint16(hwy.Float32ToFloat16(mean)))
		varAcc := asm.ZeroFloat16x8AVX2()
		ii = 0
		for ; ii+lanes <= normSize; ii += lanes {
			x := asm.LoadFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(input[off+ii:]))), len(input[off+ii:])))
			diff := x.Sub(vMean)
			varAcc = diff.MulAdd(diff, varAcc)
		}
		variance := varAcc.ReduceSum()
		for i := ii; i < normSize; i++ {
			diff := input[off+i].Float32() - mean
			variance += diff * diff
		}
		variance *= invN
		invStd := float32(1.0 / stdmath.Sqrt(float64(variance+epsilon.Float32())))
		vInvStd := asm.BroadcastFloat16x8AVX2(uint16(hwy.Float32ToFloat16(invStd)))
		if gamma != nil && beta != nil {
			ii = 0
			for ; ii+lanes <= normSize; ii += lanes {
				x := asm.LoadFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(input[off+ii:]))), len(input[off+ii:])))
				diff := x.Sub(vMean)
				normed := diff.Mul(vInvStd)
				g := asm.LoadFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(gamma[ii:]))), len(gamma[ii:])))
				b := asm.LoadFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(beta[ii:]))), len(beta[ii:])))
				result := normed.MulAdd(g, b)
				result.StoreSlice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(output[off+ii:]))), len(output[off+ii:])))
			}
			for i := ii; i < normSize; i++ {
				normed := (input[off+i].Float32() - mean) * invStd
				output[off+i] = hwy.Float32ToFloat16(normed*gamma[i].Float32() + beta[i].Float32())
			}
		} else if gamma != nil {
			ii = 0
			for ; ii+lanes <= normSize; ii += lanes {
				x := asm.LoadFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(input[off+ii:]))), len(input[off+ii:])))
				diff := x.Sub(vMean)
				normed := diff.Mul(vInvStd)
				g := asm.LoadFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(gamma[ii:]))), len(gamma[ii:])))
				result := normed.Mul(g)
				result.StoreSlice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(output[off+ii:]))), len(output[off+ii:])))
			}
			for i := ii; i < normSize; i++ {
				normed := (input[off+i].Float32() - mean) * invStd
				output[off+i] = hwy.Float32ToFloat16(normed * gamma[i].Float32())
			}
		} else {
			ii = 0
			for ; ii+lanes <= normSize; ii += lanes {
				x := asm.LoadFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(input[off+ii:]))), len(input[off+ii:])))
				diff := x.Sub(vMean)
				result := diff.Mul(vInvStd)
				result.StoreSlice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(output[off+ii:]))), len(output[off+ii:])))
			}
			for i := ii; i < normSize; i++ {
				output[off+i] = hwy.Float32ToFloat16((input[off+i].Float32() - mean) * invStd)
			}
		}
	}
}

func BaseLayerNorm_avx2_BFloat16(input []hwy.BFloat16, output []hwy.BFloat16, normSize int, gamma []hwy.BFloat16, beta []hwy.BFloat16, epsilon hwy.BFloat16) {
	size := min(len(input), len(output))
	if size == 0 || normSize <= 0 {
		return
	}
	numGroups := size / normSize
	invN := float32(1.0) / float32(normSize)
	lanes := 8
	for g := 0; g < numGroups; g++ {
		off := g * normSize
		sumAcc := asm.ZeroBFloat16x8AVX2()
		ii := 0
		for ; ii+lanes <= normSize; ii += lanes {
			x := asm.LoadBFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(input[off+ii:]))), len(input[off+ii:])))
			sumAcc = sumAcc.Add(x)
		}
		mean := sumAcc.ReduceSum()
		for i := ii; i < normSize; i++ {
			mean += input[off+i].Float32()
		}
		mean *= invN
		vMean := asm.BroadcastBFloat16x8AVX2(uint16(hwy.Float32ToBFloat16(mean)))
		varAcc := asm.ZeroBFloat16x8AVX2()
		ii = 0
		for ; ii+lanes <= normSize; ii += lanes {
			x := asm.LoadBFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(input[off+ii:]))), len(input[off+ii:])))
			diff := x.Sub(vMean)
			varAcc = diff.MulAdd(diff, varAcc)
		}
		variance := varAcc.ReduceSum()
		for i := ii; i < normSize; i++ {
			diff := input[off+i].Float32() - mean
			variance += diff * diff
		}
		variance *= invN
		invStd := float32(1.0 / stdmath.Sqrt(float64(variance+epsilon.Float32())))
		vInvStd := asm.BroadcastBFloat16x8AVX2(uint16(hwy.Float32ToBFloat16(invStd)))
		if gamma != nil && beta != nil {
			ii = 0
			for ; ii+lanes <= normSize; ii += lanes {
				x := asm.LoadBFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(input[off+ii:]))), len(input[off+ii:])))
				diff := x.Sub(vMean)
				normed := diff.Mul(vInvStd)
				g := asm.LoadBFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(gamma[ii:]))), len(gamma[ii:])))
				b := asm.LoadBFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(beta[ii:]))), len(beta[ii:])))
				result := normed.MulAdd(g, b)
				result.StoreSlice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(output[off+ii:]))), len(output[off+ii:])))
			}
			for i := ii; i < normSize; i++ {
				normed := (input[off+i].Float32() - mean) * invStd
				output[off+i] = hwy.Float32ToBFloat16(normed*gamma[i].Float32() + beta[i].Float32())
			}
		} else if gamma != nil {
			ii = 0
			for ; ii+lanes <= normSize; ii += lanes {
				x := asm.LoadBFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(input[off+ii:]))), len(input[off+ii:])))
				diff := x.Sub(vMean)
				normed := diff.Mul(vInvStd)
				g := asm.LoadBFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(gamma[ii:]))), len(gamma[ii:])))
				result := normed.Mul(g)
				result.StoreSlice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(output[off+ii:]))), len(output[off+ii:])))
			}
			for i := ii; i < normSize; i++ {
				normed := (input[off+i].Float32() - mean) * invStd
				output[off+i] = hwy.Float32ToBFloat16(normed * gamma[i].Float32())
			}
		} else {
			ii = 0
			for ; ii+lanes <= normSize; ii += lanes {
				x := asm.LoadBFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(input[off+ii:]))), len(input[off+ii:])))
				diff := x.Sub(vMean)
				result := diff.Mul(vInvStd)
				result.StoreSlice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(output[off+ii:]))), len(output[off+ii:])))
			}
			for i := ii; i < normSize; i++ {
				output[off+i] = hwy.Float32ToBFloat16((input[off+i].Float32() - mean) * invStd)
			}
		}
	}
}

func BaseLayerNorm_avx2(input []float32, output []float32, normSize int, gamma []float32, beta []float32, epsilon float32) {
	size := min(len(input), len(output))
	if size == 0 || normSize <= 0 {
		return
	}
	numGroups := size / normSize
	invN := float32(1.0) / float32(normSize)
	lanes := 8
	for g := 0; g < numGroups; g++ {
		off := g * normSize
		sumAcc := archsimd.BroadcastFloat32x8(0)
		ii := 0
		for ; ii+lanes <= normSize; ii += lanes {
			x := archsimd.LoadFloat32x8((*[8]float32)(unsafe.Pointer(&input[off+ii])))
			sumAcc = sumAcc.Add(x)
		}
		mean := hwy.ReduceSum_AVX2_F32x8(sumAcc)
		for i := ii; i < normSize; i++ {
			mean += input[off+i]
		}
		mean *= invN
		vMean := archsimd.BroadcastFloat32x8(mean)
		varAcc := archsimd.BroadcastFloat32x8(0)
		ii = 0
		for ; ii+lanes <= normSize; ii += lanes {
			x := archsimd.LoadFloat32x8((*[8]float32)(unsafe.Pointer(&input[off+ii])))
			diff := x.Sub(vMean)
			varAcc = diff.MulAdd(diff, varAcc)
		}
		variance := hwy.ReduceSum_AVX2_F32x8(varAcc)
		for i := ii; i < normSize; i++ {
			diff := input[off+i] - mean
			variance += diff * diff
		}
		variance *= invN
		invStd := float32(1.0 / stdmath.Sqrt(float64(variance+epsilon)))
		vInvStd := archsimd.BroadcastFloat32x8(invStd)
		if gamma != nil && beta != nil {
			ii = 0
			for ; ii+lanes <= normSize; ii += lanes {
				x := archsimd.LoadFloat32x8((*[8]float32)(unsafe.Pointer(&input[off+ii])))
				diff := x.Sub(vMean)
				normed := diff.Mul(vInvStd)
				g := archsimd.LoadFloat32x8((*[8]float32)(unsafe.Pointer(&gamma[ii])))
				b := archsimd.LoadFloat32x8((*[8]float32)(unsafe.Pointer(&beta[ii])))
				result := normed.MulAdd(g, b)
				result.Store((*[8]float32)(unsafe.Pointer(&output[off+ii])))
			}
			for i := ii; i < normSize; i++ {
				normed := (input[off+i] - mean) * invStd
				output[off+i] = normed*gamma[i] + beta[i]
			}
		} else if gamma != nil {
			ii = 0
			for ; ii+lanes <= normSize; ii += lanes {
				x := archsimd.LoadFloat32x8((*[8]float32)(unsafe.Pointer(&input[off+ii])))
				diff := x.Sub(vMean)
				normed := diff.Mul(vInvStd)
				g := archsimd.LoadFloat32x8((*[8]float32)(unsafe.Pointer(&gamma[ii])))
				result := normed.Mul(g)
				result.Store((*[8]float32)(unsafe.Pointer(&output[off+ii])))
			}
			for i := ii; i < normSize; i++ {
				normed := (input[off+i] - mean) * invStd
				output[off+i] = normed * gamma[i]
			}
		} else {
			ii = 0
			for ; ii+lanes <= normSize; ii += lanes {
				x := archsimd.LoadFloat32x8((*[8]float32)(unsafe.Pointer(&input[off+ii])))
				diff := x.Sub(vMean)
				result := diff.Mul(vInvStd)
				result.Store((*[8]float32)(unsafe.Pointer(&output[off+ii])))
			}
			for i := ii; i < normSize; i++ {
				output[off+i] = (input[off+i] - mean) * invStd
			}
		}
	}
}

func BaseLayerNorm_avx2_Float64(input []float64, output []float64, normSize int, gamma []float64, beta []float64, epsilon float64) {
	size := min(len(input), len(output))
	if size == 0 || normSize <= 0 {
		return
	}
	numGroups := size / normSize
	invN := float64(1.0) / float64(normSize)
	lanes := 4
	for g := 0; g < numGroups; g++ {
		off := g * normSize
		sumAcc := archsimd.BroadcastFloat64x4(0)
		ii := 0
		for ; ii+lanes <= normSize; ii += lanes {
			x := archsimd.LoadFloat64x4((*[4]float64)(unsafe.Pointer(&input[off+ii])))
			sumAcc = sumAcc.Add(x)
		}
		mean := hwy.ReduceSum_AVX2_F64x4(sumAcc)
		for i := ii; i < normSize; i++ {
			mean += input[off+i]
		}
		mean *= invN
		vMean := archsimd.BroadcastFloat64x4(mean)
		varAcc := archsimd.BroadcastFloat64x4(0)
		ii = 0
		for ; ii+lanes <= normSize; ii += lanes {
			x := archsimd.LoadFloat64x4((*[4]float64)(unsafe.Pointer(&input[off+ii])))
			diff := x.Sub(vMean)
			varAcc = diff.MulAdd(diff, varAcc)
		}
		variance := hwy.ReduceSum_AVX2_F64x4(varAcc)
		for i := ii; i < normSize; i++ {
			diff := input[off+i] - mean
			variance += diff * diff
		}
		variance *= invN
		invStd := float64(1.0 / stdmath.Sqrt(float64(variance+epsilon)))
		vInvStd := archsimd.BroadcastFloat64x4(invStd)
		if gamma != nil && beta != nil {
			ii = 0
			for ; ii+lanes <= normSize; ii += lanes {
				x := archsimd.LoadFloat64x4((*[4]float64)(unsafe.Pointer(&input[off+ii])))
				diff := x.Sub(vMean)
				normed := diff.Mul(vInvStd)
				g := archsimd.LoadFloat64x4((*[4]float64)(unsafe.Pointer(&gamma[ii])))
				b := archsimd.LoadFloat64x4((*[4]float64)(unsafe.Pointer(&beta[ii])))
				result := normed.MulAdd(g, b)
				result.Store((*[4]float64)(unsafe.Pointer(&output[off+ii])))
			}
			for i := ii; i < normSize; i++ {
				normed := (input[off+i] - mean) * invStd
				output[off+i] = normed*gamma[i] + beta[i]
			}
		} else if gamma != nil {
			ii = 0
			for ; ii+lanes <= normSize; ii += lanes {
				x := archsimd.LoadFloat64x4((*[4]float64)(unsafe.Pointer(&input[off+ii])))
				diff := x.Sub(vMean)
				normed := diff.Mul(vInvStd)
				g := archsimd.LoadFloat64x4((*[4]float64)(unsafe.Pointer(&gamma[ii])))
				result := normed.Mul(g)
				result.Store((*[4]float64)(unsafe.Pointer(&output[off+ii])))
			}
			for i := ii; i < normSize; i++ {
				normed := (input[off+i] - mean) * invStd
				output[off+i] = normed * gamma[i]
			}
		} else {
			ii = 0
			for ; ii+lanes <= normSize; ii += lanes {
				x := archsimd.LoadFloat64x4((*[4]float64)(unsafe.Pointer(&input[off+ii])))
				diff := x.Sub(vMean)
				result := diff.Mul(vInvStd)
				result.Store((*[4]float64)(unsafe.Pointer(&output[off+ii])))
			}
			for i := ii; i < normSize; i++ {
				output[off+i] = (input[off+i] - mean) * invStd
			}
		}
	}
}
