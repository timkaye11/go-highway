// Code generated by github.com/ajroetker/go-highway/cmd/hwygen. DO NOT EDIT.

//go:build arm64

package nn

import (
	"unsafe"

	"github.com/ajroetker/go-highway/hwy"
	"github.com/ajroetker/go-highway/hwy/asm"
)

func BaseRoPE_neon_Float16(data []hwy.Float16, cos []hwy.Float16, sin []hwy.Float16, seqLen int, headDim int) {
	if seqLen == 0 || headDim == 0 {
		return
	}
	halfDim := headDim / 2
	lanes := 8
	for pos := range seqLen {
		dataOff := pos * headDim
		csOff := pos * halfDim
		first := data[dataOff : dataOff+halfDim]
		second := data[dataOff+halfDim : dataOff+headDim]
		c := cos[csOff : csOff+halfDim]
		s := sin[csOff : csOff+halfDim]
		ii := 0
		for ; ii+lanes <= halfDim; ii += lanes {
			f := asm.LoadFloat16x8Ptr(unsafe.Pointer(&first[ii:][0]))
			se := asm.LoadFloat16x8Ptr(unsafe.Pointer(&second[ii:][0]))
			cv := asm.LoadFloat16x8Ptr(unsafe.Pointer(&c[ii:][0]))
			sv := asm.LoadFloat16x8Ptr(unsafe.Pointer(&s[ii:][0]))
			newFirst := f.Mul(cv).Sub(se.Mul(sv))
			newSecond := f.MulAdd(sv, se.Mul(cv))
			newFirst.StorePtr(unsafe.Pointer(&first[ii:][0]))
			newSecond.StorePtr(unsafe.Pointer(&second[ii:][0]))
		}
		for d := ii; d < halfDim; d++ {
			f := first[d]
			se := second[d]
			first[d] = hwy.Float32ToFloat16(f.Float32()*c[d].Float32() - se.Float32()*s[d].Float32())
			second[d] = hwy.Float32ToFloat16(f.Float32()*s[d].Float32() + se.Float32()*c[d].Float32())
		}
	}
}

func BaseRoPE_neon_BFloat16(data []hwy.BFloat16, cos []hwy.BFloat16, sin []hwy.BFloat16, seqLen int, headDim int) {
	if seqLen == 0 || headDim == 0 {
		return
	}
	halfDim := headDim / 2
	lanes := 8
	for pos := range seqLen {
		dataOff := pos * headDim
		csOff := pos * halfDim
		first := data[dataOff : dataOff+halfDim]
		second := data[dataOff+halfDim : dataOff+headDim]
		c := cos[csOff : csOff+halfDim]
		s := sin[csOff : csOff+halfDim]
		ii := 0
		for ; ii+lanes <= halfDim; ii += lanes {
			f := asm.LoadBFloat16x8Ptr(unsafe.Pointer(&first[ii:][0]))
			se := asm.LoadBFloat16x8Ptr(unsafe.Pointer(&second[ii:][0]))
			cv := asm.LoadBFloat16x8Ptr(unsafe.Pointer(&c[ii:][0]))
			sv := asm.LoadBFloat16x8Ptr(unsafe.Pointer(&s[ii:][0]))
			newFirst := f.Mul(cv).Sub(se.Mul(sv))
			newSecond := f.MulAdd(sv, se.Mul(cv))
			newFirst.StorePtr(unsafe.Pointer(&first[ii:][0]))
			newSecond.StorePtr(unsafe.Pointer(&second[ii:][0]))
		}
		for d := ii; d < halfDim; d++ {
			f := first[d]
			se := second[d]
			first[d] = hwy.Float32ToBFloat16(f.Float32()*c[d].Float32() - se.Float32()*s[d].Float32())
			second[d] = hwy.Float32ToBFloat16(f.Float32()*s[d].Float32() + se.Float32()*c[d].Float32())
		}
	}
}

func BaseRoPE_neon(data []float32, cos []float32, sin []float32, seqLen int, headDim int) {
	if seqLen == 0 || headDim == 0 {
		return
	}
	halfDim := headDim / 2
	lanes := 4
	for pos := range seqLen {
		dataOff := pos * headDim
		csOff := pos * halfDim
		first := data[dataOff : dataOff+halfDim]
		second := data[dataOff+halfDim : dataOff+headDim]
		c := cos[csOff : csOff+halfDim]
		s := sin[csOff : csOff+halfDim]
		ii := 0
		for ; ii+lanes <= halfDim; ii += lanes {
			f := asm.LoadFloat32x4((*[4]float32)(unsafe.Pointer(&first[ii])))
			se := asm.LoadFloat32x4((*[4]float32)(unsafe.Pointer(&second[ii])))
			cv := asm.LoadFloat32x4((*[4]float32)(unsafe.Pointer(&c[ii])))
			sv := asm.LoadFloat32x4((*[4]float32)(unsafe.Pointer(&s[ii])))
			newFirst := f.Mul(cv).Sub(se.Mul(sv))
			newSecond := f.MulAdd(sv, se.Mul(cv))
			newFirst.Store((*[4]float32)(unsafe.Pointer(&first[ii])))
			newSecond.Store((*[4]float32)(unsafe.Pointer(&second[ii])))
		}
		for d := ii; d < halfDim; d++ {
			f := first[d]
			se := second[d]
			first[d] = f*c[d] - se*s[d]
			second[d] = f*s[d] + se*c[d]
		}
	}
}

func BaseRoPE_neon_Float64(data []float64, cos []float64, sin []float64, seqLen int, headDim int) {
	if seqLen == 0 || headDim == 0 {
		return
	}
	halfDim := headDim / 2
	lanes := 2
	for pos := range seqLen {
		dataOff := pos * headDim
		csOff := pos * halfDim
		first := data[dataOff : dataOff+halfDim]
		second := data[dataOff+halfDim : dataOff+headDim]
		c := cos[csOff : csOff+halfDim]
		s := sin[csOff : csOff+halfDim]
		ii := 0
		for ; ii+lanes <= halfDim; ii += lanes {
			f := asm.LoadFloat64x2((*[2]float64)(unsafe.Pointer(&first[ii])))
			se := asm.LoadFloat64x2((*[2]float64)(unsafe.Pointer(&second[ii])))
			cv := asm.LoadFloat64x2((*[2]float64)(unsafe.Pointer(&c[ii])))
			sv := asm.LoadFloat64x2((*[2]float64)(unsafe.Pointer(&s[ii])))
			newFirst := f.Mul(cv).Sub(se.Mul(sv))
			newSecond := f.MulAdd(sv, se.Mul(cv))
			newFirst.Store((*[2]float64)(unsafe.Pointer(&first[ii])))
			newSecond.Store((*[2]float64)(unsafe.Pointer(&second[ii])))
		}
		for d := ii; d < halfDim; d++ {
			f := first[d]
			se := second[d]
			first[d] = f*c[d] - se*s[d]
			second[d] = f*s[d] + se*c[d]
		}
	}
}
