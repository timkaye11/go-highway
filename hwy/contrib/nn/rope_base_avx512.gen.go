// Code generated by github.com/ajroetker/go-highway/cmd/hwygen. DO NOT EDIT.

//go:build amd64 && goexperiment.simd

package nn

import (
	"simd/archsimd"
	"unsafe"

	"github.com/ajroetker/go-highway/hwy"
	"github.com/ajroetker/go-highway/hwy/asm"
)

func BaseRoPE_avx512_Float16(data []hwy.Float16, cos []hwy.Float16, sin []hwy.Float16, seqLen int, headDim int) {
	if seqLen == 0 || headDim == 0 {
		return
	}
	halfDim := headDim / 2
	lanes := 16
	for pos := range seqLen {
		dataOff := pos * headDim
		csOff := pos * halfDim
		first := data[dataOff : dataOff+halfDim]
		second := data[dataOff+halfDim : dataOff+headDim]
		c := cos[csOff : csOff+halfDim]
		s := sin[csOff : csOff+halfDim]
		ii := 0
		for ; ii+lanes <= halfDim; ii += lanes {
			f := asm.LoadFloat16x16AVX512Ptr(unsafe.Pointer(&first[ii:][0]))
			se := asm.LoadFloat16x16AVX512Ptr(unsafe.Pointer(&second[ii:][0]))
			cv := asm.LoadFloat16x16AVX512Ptr(unsafe.Pointer(&c[ii:][0]))
			sv := asm.LoadFloat16x16AVX512Ptr(unsafe.Pointer(&s[ii:][0]))
			newFirst := f.Mul(cv).Sub(se.Mul(sv))
			newSecond := f.MulAdd(sv, se.Mul(cv))
			newFirst.StorePtr(unsafe.Pointer(&first[ii:][0]))
			newSecond.StorePtr(unsafe.Pointer(&second[ii:][0]))
		}
		for d := ii; d < halfDim; d++ {
			f := first[d]
			se := second[d]
			first[d] = hwy.Float32ToFloat16(f.Float32()*c[d].Float32() - se.Float32()*s[d].Float32())
			second[d] = hwy.Float32ToFloat16(f.Float32()*s[d].Float32() + se.Float32()*c[d].Float32())
		}
	}
}

func BaseRoPE_avx512_BFloat16(data []hwy.BFloat16, cos []hwy.BFloat16, sin []hwy.BFloat16, seqLen int, headDim int) {
	if seqLen == 0 || headDim == 0 {
		return
	}
	halfDim := headDim / 2
	lanes := 16
	for pos := range seqLen {
		dataOff := pos * headDim
		csOff := pos * halfDim
		first := data[dataOff : dataOff+halfDim]
		second := data[dataOff+halfDim : dataOff+headDim]
		c := cos[csOff : csOff+halfDim]
		s := sin[csOff : csOff+halfDim]
		ii := 0
		for ; ii+lanes <= halfDim; ii += lanes {
			f := asm.LoadBFloat16x16AVX512Ptr(unsafe.Pointer(&first[ii:][0]))
			se := asm.LoadBFloat16x16AVX512Ptr(unsafe.Pointer(&second[ii:][0]))
			cv := asm.LoadBFloat16x16AVX512Ptr(unsafe.Pointer(&c[ii:][0]))
			sv := asm.LoadBFloat16x16AVX512Ptr(unsafe.Pointer(&s[ii:][0]))
			newFirst := f.Mul(cv).Sub(se.Mul(sv))
			newSecond := f.MulAdd(sv, se.Mul(cv))
			newFirst.StorePtr(unsafe.Pointer(&first[ii:][0]))
			newSecond.StorePtr(unsafe.Pointer(&second[ii:][0]))
		}
		for d := ii; d < halfDim; d++ {
			f := first[d]
			se := second[d]
			first[d] = hwy.Float32ToBFloat16(f.Float32()*c[d].Float32() - se.Float32()*s[d].Float32())
			second[d] = hwy.Float32ToBFloat16(f.Float32()*s[d].Float32() + se.Float32()*c[d].Float32())
		}
	}
}

func BaseRoPE_avx512(data []float32, cos []float32, sin []float32, seqLen int, headDim int) {
	if seqLen == 0 || headDim == 0 {
		return
	}
	halfDim := headDim / 2
	lanes := 16
	for pos := range seqLen {
		dataOff := pos * headDim
		csOff := pos * halfDim
		first := data[dataOff : dataOff+halfDim]
		second := data[dataOff+halfDim : dataOff+headDim]
		c := cos[csOff : csOff+halfDim]
		s := sin[csOff : csOff+halfDim]
		ii := 0
		for ; ii+lanes <= halfDim; ii += lanes {
			f := archsimd.LoadFloat32x16((*[16]float32)(unsafe.Pointer(&first[ii])))
			se := archsimd.LoadFloat32x16((*[16]float32)(unsafe.Pointer(&second[ii])))
			cv := archsimd.LoadFloat32x16((*[16]float32)(unsafe.Pointer(&c[ii])))
			sv := archsimd.LoadFloat32x16((*[16]float32)(unsafe.Pointer(&s[ii])))
			newFirst := f.Mul(cv).Sub(se.Mul(sv))
			newSecond := f.MulAdd(sv, se.Mul(cv))
			newFirst.Store((*[16]float32)(unsafe.Pointer(&first[ii])))
			newSecond.Store((*[16]float32)(unsafe.Pointer(&second[ii])))
		}
		for d := ii; d < halfDim; d++ {
			f := first[d]
			se := second[d]
			first[d] = f*c[d] - se*s[d]
			second[d] = f*s[d] + se*c[d]
		}
	}
}

func BaseRoPE_avx512_Float64(data []float64, cos []float64, sin []float64, seqLen int, headDim int) {
	if seqLen == 0 || headDim == 0 {
		return
	}
	halfDim := headDim / 2
	lanes := 8
	for pos := range seqLen {
		dataOff := pos * headDim
		csOff := pos * halfDim
		first := data[dataOff : dataOff+halfDim]
		second := data[dataOff+halfDim : dataOff+headDim]
		c := cos[csOff : csOff+halfDim]
		s := sin[csOff : csOff+halfDim]
		ii := 0
		for ; ii+lanes <= halfDim; ii += lanes {
			f := archsimd.LoadFloat64x8((*[8]float64)(unsafe.Pointer(&first[ii])))
			se := archsimd.LoadFloat64x8((*[8]float64)(unsafe.Pointer(&second[ii])))
			cv := archsimd.LoadFloat64x8((*[8]float64)(unsafe.Pointer(&c[ii])))
			sv := archsimd.LoadFloat64x8((*[8]float64)(unsafe.Pointer(&s[ii])))
			newFirst := f.Mul(cv).Sub(se.Mul(sv))
			newSecond := f.MulAdd(sv, se.Mul(cv))
			newFirst.Store((*[8]float64)(unsafe.Pointer(&first[ii])))
			newSecond.Store((*[8]float64)(unsafe.Pointer(&second[ii])))
		}
		for d := ii; d < halfDim; d++ {
			f := first[d]
			se := second[d]
			first[d] = f*c[d] - se*s[d]
			second[d] = f*s[d] + se*c[d]
		}
	}
}
