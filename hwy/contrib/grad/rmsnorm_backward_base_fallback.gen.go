// Code generated by github.com/ajroetker/go-highway/cmd/hwygen. DO NOT EDIT.

package grad

import (
	"github.com/ajroetker/go-highway/hwy"
)

func BaseRMSNormBackward_fallback_Float16(gradOutput []hwy.Float16, savedInput []hwy.Float16, savedRRMS []hwy.Float16, weight []hwy.Float16, normSize int, gemmaMode bool, gradInput []hwy.Float16, gradWeight []hwy.Float16) {
	size := min(len(gradOutput), min(len(savedInput), len(gradInput)))
	if size == 0 || normSize <= 0 {
		return
	}
	numGroups := size / normSize
	lanes := hwy.MaxLanes[hwy.Float16]()
	for g := range numGroups {
		off := g * normSize
		rrms := savedRRMS[g]
		dotAcc := hwy.Zero[hwy.Float16]()
		ii := 0
		if weight != nil && gemmaMode {
			vOne := hwy.Const[hwy.Float16](1.0)
			vRRMS := hwy.Set(rrms)
			for ; ii+lanes <= normSize; ii += lanes {
				gOut := hwy.Load(gradOutput[off+ii:])
				x := hwy.Load(savedInput[off+ii:])
				w := hwy.Load(weight[ii:])
				wEff := hwy.Add(w, vOne)
				contrib := hwy.Mul(hwy.Mul(gOut, wEff), x)
				dotAcc = hwy.Add(dotAcc, contrib)
				if gradWeight != nil {
					gw := hwy.Load(gradWeight[ii:])
					xRrms := hwy.Mul(x, vRRMS)
					newGW := hwy.MulAdd(gOut, xRrms, gw)
					hwy.Store(newGW, gradWeight[ii:])
				}
			}
			dot := hwy.ReduceSum(dotAcc).Float32()
			for i := ii; i < normSize; i++ {
				wEff := weight[i].Float32() + 1.0
				dot += gradOutput[off+i].Float32() * wEff * savedInput[off+i].Float32()
				if gradWeight != nil {
					gradWeight[i] = hwy.Float32ToFloat16(gradWeight[i].Float32() + gradOutput[off+i].Float32()*savedInput[off+i].Float32()*rrms.Float32())
				}
			}
			invN := float32(1.0) / float32(normSize)
			dotCoeff := dot * rrms.Float32() * rrms.Float32() * rrms.Float32() * invN
			vDotCoeff := hwy.Set(hwy.Float32ToFloat16(dotCoeff))
			vRRMS2 := hwy.Set(rrms)
			ii = 0
			for ; ii+lanes <= normSize; ii += lanes {
				gOut := hwy.Load(gradOutput[off+ii:])
				x := hwy.Load(savedInput[off+ii:])
				gIn := hwy.Load(gradInput[off+ii:])
				w := hwy.Load(weight[ii:])
				wEff := hwy.Add(w, vOne)
				term1 := hwy.Mul(hwy.Mul(gOut, wEff), vRRMS2)
				term2 := hwy.Mul(x, vDotCoeff)
				contribGI := hwy.Sub(term1, term2)
				result := hwy.Add(gIn, contribGI)
				hwy.Store(result, gradInput[off+ii:])
			}
			for i := ii; i < normSize; i++ {
				wEff := weight[i].Float32() + 1.0
				gradInput[off+i] = hwy.Float32ToFloat16(gradInput[off+i].Float32() + (rrms.Float32()*gradOutput[off+i].Float32()*wEff - savedInput[off+i].Float32()*dotCoeff))
			}
		} else if weight != nil {
			vRRMS := hwy.Set(rrms)
			for ; ii+lanes <= normSize; ii += lanes {
				gOut := hwy.Load(gradOutput[off+ii:])
				x := hwy.Load(savedInput[off+ii:])
				w := hwy.Load(weight[ii:])
				contrib := hwy.Mul(hwy.Mul(gOut, w), x)
				dotAcc = hwy.Add(dotAcc, contrib)
				if gradWeight != nil {
					gwt := hwy.Load(gradWeight[ii:])
					xRrms := hwy.Mul(x, vRRMS)
					newGWT := hwy.MulAdd(gOut, xRrms, gwt)
					hwy.Store(newGWT, gradWeight[ii:])
				}
			}
			dot := hwy.ReduceSum(dotAcc).Float32()
			for i := ii; i < normSize; i++ {
				dot += gradOutput[off+i].Float32() * weight[i].Float32() * savedInput[off+i].Float32()
				if gradWeight != nil {
					gradWeight[i] = hwy.Float32ToFloat16(gradWeight[i].Float32() + gradOutput[off+i].Float32()*savedInput[off+i].Float32()*rrms.Float32())
				}
			}
			invN := float32(1.0) / float32(normSize)
			dotCoeff := dot * rrms.Float32() * rrms.Float32() * rrms.Float32() * invN
			vDotCoeff := hwy.Set(hwy.Float32ToFloat16(dotCoeff))
			vRRMS2 := hwy.Set(rrms)
			ii = 0
			for ; ii+lanes <= normSize; ii += lanes {
				gOut := hwy.Load(gradOutput[off+ii:])
				x := hwy.Load(savedInput[off+ii:])
				gIn := hwy.Load(gradInput[off+ii:])
				w := hwy.Load(weight[ii:])
				term1 := hwy.Mul(hwy.Mul(gOut, w), vRRMS2)
				term2 := hwy.Mul(x, vDotCoeff)
				contribGI := hwy.Sub(term1, term2)
				result := hwy.Add(gIn, contribGI)
				hwy.Store(result, gradInput[off+ii:])
			}
			for i := ii; i < normSize; i++ {
				gradInput[off+i] = hwy.Float32ToFloat16(gradInput[off+i].Float32() + (rrms.Float32()*gradOutput[off+i].Float32()*weight[i].Float32() - savedInput[off+i].Float32()*dotCoeff))
			}
		} else {
			for ; ii+lanes <= normSize; ii += lanes {
				gOut := hwy.Load(gradOutput[off+ii:])
				x := hwy.Load(savedInput[off+ii:])
				contrib := hwy.Mul(gOut, x)
				dotAcc = hwy.Add(dotAcc, contrib)
			}
			dot := hwy.ReduceSum(dotAcc).Float32()
			for i := ii; i < normSize; i++ {
				dot += gradOutput[off+i].Float32() * savedInput[off+i].Float32()
			}
			invN := float32(1.0) / float32(normSize)
			dotCoeff := dot * rrms.Float32() * rrms.Float32() * rrms.Float32() * invN
			vDotCoeff := hwy.Set(hwy.Float32ToFloat16(dotCoeff))
			vRRMS := hwy.Set(rrms)
			ii = 0
			for ; ii+lanes <= normSize; ii += lanes {
				gOut := hwy.Load(gradOutput[off+ii:])
				x := hwy.Load(savedInput[off+ii:])
				gIn := hwy.Load(gradInput[off+ii:])
				term1 := hwy.Mul(gOut, vRRMS)
				term2 := hwy.Mul(x, vDotCoeff)
				contribGI := hwy.Sub(term1, term2)
				result := hwy.Add(gIn, contribGI)
				hwy.Store(result, gradInput[off+ii:])
			}
			for i := ii; i < normSize; i++ {
				gradInput[off+i] = hwy.Float32ToFloat16(gradInput[off+i].Float32() + (rrms.Float32()*gradOutput[off+i].Float32() - savedInput[off+i].Float32()*dotCoeff))
			}
		}
	}
}

func BaseRMSNormBackward_fallback_BFloat16(gradOutput []hwy.BFloat16, savedInput []hwy.BFloat16, savedRRMS []hwy.BFloat16, weight []hwy.BFloat16, normSize int, gemmaMode bool, gradInput []hwy.BFloat16, gradWeight []hwy.BFloat16) {
	size := min(len(gradOutput), min(len(savedInput), len(gradInput)))
	if size == 0 || normSize <= 0 {
		return
	}
	numGroups := size / normSize
	lanes := hwy.MaxLanes[hwy.BFloat16]()
	for g := range numGroups {
		off := g * normSize
		rrms := savedRRMS[g]
		dotAcc := hwy.Zero[hwy.BFloat16]()
		ii := 0
		if weight != nil && gemmaMode {
			vOne := hwy.Const[hwy.BFloat16](1.0)
			vRRMS := hwy.Set(rrms)
			for ; ii+lanes <= normSize; ii += lanes {
				gOut := hwy.Load(gradOutput[off+ii:])
				x := hwy.Load(savedInput[off+ii:])
				w := hwy.Load(weight[ii:])
				wEff := hwy.Add(w, vOne)
				contrib := hwy.Mul(hwy.Mul(gOut, wEff), x)
				dotAcc = hwy.Add(dotAcc, contrib)
				if gradWeight != nil {
					gw := hwy.Load(gradWeight[ii:])
					xRrms := hwy.Mul(x, vRRMS)
					newGW := hwy.MulAdd(gOut, xRrms, gw)
					hwy.Store(newGW, gradWeight[ii:])
				}
			}
			dot := hwy.ReduceSum(dotAcc).Float32()
			for i := ii; i < normSize; i++ {
				wEff := weight[i].Float32() + 1.0
				dot += gradOutput[off+i].Float32() * wEff * savedInput[off+i].Float32()
				if gradWeight != nil {
					gradWeight[i] = hwy.Float32ToBFloat16(gradWeight[i].Float32() + gradOutput[off+i].Float32()*savedInput[off+i].Float32()*rrms.Float32())
				}
			}
			invN := float32(1.0) / float32(normSize)
			dotCoeff := dot * rrms.Float32() * rrms.Float32() * rrms.Float32() * invN
			vDotCoeff := hwy.Set(hwy.Float32ToBFloat16(dotCoeff))
			vRRMS2 := hwy.Set(rrms)
			ii = 0
			for ; ii+lanes <= normSize; ii += lanes {
				gOut := hwy.Load(gradOutput[off+ii:])
				x := hwy.Load(savedInput[off+ii:])
				gIn := hwy.Load(gradInput[off+ii:])
				w := hwy.Load(weight[ii:])
				wEff := hwy.Add(w, vOne)
				term1 := hwy.Mul(hwy.Mul(gOut, wEff), vRRMS2)
				term2 := hwy.Mul(x, vDotCoeff)
				contribGI := hwy.Sub(term1, term2)
				result := hwy.Add(gIn, contribGI)
				hwy.Store(result, gradInput[off+ii:])
			}
			for i := ii; i < normSize; i++ {
				wEff := weight[i].Float32() + 1.0
				gradInput[off+i] = hwy.Float32ToBFloat16(gradInput[off+i].Float32() + (rrms.Float32()*gradOutput[off+i].Float32()*wEff - savedInput[off+i].Float32()*dotCoeff))
			}
		} else if weight != nil {
			vRRMS := hwy.Set(rrms)
			for ; ii+lanes <= normSize; ii += lanes {
				gOut := hwy.Load(gradOutput[off+ii:])
				x := hwy.Load(savedInput[off+ii:])
				w := hwy.Load(weight[ii:])
				contrib := hwy.Mul(hwy.Mul(gOut, w), x)
				dotAcc = hwy.Add(dotAcc, contrib)
				if gradWeight != nil {
					gwt := hwy.Load(gradWeight[ii:])
					xRrms := hwy.Mul(x, vRRMS)
					newGWT := hwy.MulAdd(gOut, xRrms, gwt)
					hwy.Store(newGWT, gradWeight[ii:])
				}
			}
			dot := hwy.ReduceSum(dotAcc).Float32()
			for i := ii; i < normSize; i++ {
				dot += gradOutput[off+i].Float32() * weight[i].Float32() * savedInput[off+i].Float32()
				if gradWeight != nil {
					gradWeight[i] = hwy.Float32ToBFloat16(gradWeight[i].Float32() + gradOutput[off+i].Float32()*savedInput[off+i].Float32()*rrms.Float32())
				}
			}
			invN := float32(1.0) / float32(normSize)
			dotCoeff := dot * rrms.Float32() * rrms.Float32() * rrms.Float32() * invN
			vDotCoeff := hwy.Set(hwy.Float32ToBFloat16(dotCoeff))
			vRRMS2 := hwy.Set(rrms)
			ii = 0
			for ; ii+lanes <= normSize; ii += lanes {
				gOut := hwy.Load(gradOutput[off+ii:])
				x := hwy.Load(savedInput[off+ii:])
				gIn := hwy.Load(gradInput[off+ii:])
				w := hwy.Load(weight[ii:])
				term1 := hwy.Mul(hwy.Mul(gOut, w), vRRMS2)
				term2 := hwy.Mul(x, vDotCoeff)
				contribGI := hwy.Sub(term1, term2)
				result := hwy.Add(gIn, contribGI)
				hwy.Store(result, gradInput[off+ii:])
			}
			for i := ii; i < normSize; i++ {
				gradInput[off+i] = hwy.Float32ToBFloat16(gradInput[off+i].Float32() + (rrms.Float32()*gradOutput[off+i].Float32()*weight[i].Float32() - savedInput[off+i].Float32()*dotCoeff))
			}
		} else {
			for ; ii+lanes <= normSize; ii += lanes {
				gOut := hwy.Load(gradOutput[off+ii:])
				x := hwy.Load(savedInput[off+ii:])
				contrib := hwy.Mul(gOut, x)
				dotAcc = hwy.Add(dotAcc, contrib)
			}
			dot := hwy.ReduceSum(dotAcc).Float32()
			for i := ii; i < normSize; i++ {
				dot += gradOutput[off+i].Float32() * savedInput[off+i].Float32()
			}
			invN := float32(1.0) / float32(normSize)
			dotCoeff := dot * rrms.Float32() * rrms.Float32() * rrms.Float32() * invN
			vDotCoeff := hwy.Set(hwy.Float32ToBFloat16(dotCoeff))
			vRRMS := hwy.Set(rrms)
			ii = 0
			for ; ii+lanes <= normSize; ii += lanes {
				gOut := hwy.Load(gradOutput[off+ii:])
				x := hwy.Load(savedInput[off+ii:])
				gIn := hwy.Load(gradInput[off+ii:])
				term1 := hwy.Mul(gOut, vRRMS)
				term2 := hwy.Mul(x, vDotCoeff)
				contribGI := hwy.Sub(term1, term2)
				result := hwy.Add(gIn, contribGI)
				hwy.Store(result, gradInput[off+ii:])
			}
			for i := ii; i < normSize; i++ {
				gradInput[off+i] = hwy.Float32ToBFloat16(gradInput[off+i].Float32() + (rrms.Float32()*gradOutput[off+i].Float32() - savedInput[off+i].Float32()*dotCoeff))
			}
		}
	}
}

func BaseRMSNormBackward_fallback(gradOutput []float32, savedInput []float32, savedRRMS []float32, weight []float32, normSize int, gemmaMode bool, gradInput []float32, gradWeight []float32) {
	size := min(len(gradOutput), min(len(savedInput), len(gradInput)))
	if size == 0 || normSize <= 0 {
		return
	}
	numGroups := size / normSize
	for g := range numGroups {
		off := g * normSize
		rrms := savedRRMS[g]
		dotAcc := float32(0)
		ii := 0
		if weight != nil && gemmaMode {
			vOne := float32(1.0)
			vRRMS := float32(rrms)
			for ; ii < normSize; ii++ {
				gOut := gradOutput[off+ii]
				x := savedInput[off+ii]
				w := weight[ii]
				wEff := w + vOne
				contrib := gOut * wEff * x
				dotAcc = dotAcc + contrib
				if gradWeight != nil {
					gw := gradWeight[ii]
					xRrms := x * vRRMS
					newGW := gOut*xRrms + gw
					gradWeight[ii] = newGW
				}
			}
			dot := dotAcc
			for i := ii; i < normSize; i++ {
				wEff := weight[i] + 1.0
				dot += gradOutput[off+i] * wEff * savedInput[off+i]
				if gradWeight != nil {
					gradWeight[i] += gradOutput[off+i] * savedInput[off+i] * rrms
				}
			}
			invN := float32(1.0) / float32(normSize)
			dotCoeff := dot * rrms * rrms * rrms * invN
			vDotCoeff := float32(dotCoeff)
			vRRMS2 := float32(rrms)
			ii = 0
			for ; ii < normSize; ii++ {
				gOut := gradOutput[off+ii]
				x := savedInput[off+ii]
				gIn := gradInput[off+ii]
				w := weight[ii]
				wEff := w + vOne
				term1 := gOut * wEff * vRRMS2
				term2 := x * vDotCoeff
				contribGI := term1 - term2
				result := gIn + contribGI
				gradInput[off+ii] = result
			}
			for i := ii; i < normSize; i++ {
				wEff := weight[i] + 1.0
				gradInput[off+i] += rrms*gradOutput[off+i]*wEff - savedInput[off+i]*dotCoeff
			}
		} else if weight != nil {
			vRRMS := float32(rrms)
			for ; ii < normSize; ii++ {
				gOut := gradOutput[off+ii]
				x := savedInput[off+ii]
				w := weight[ii]
				contrib := gOut * w * x
				dotAcc = dotAcc + contrib
				if gradWeight != nil {
					gwt := gradWeight[ii]
					xRrms := x * vRRMS
					newGWT := gOut*xRrms + gwt
					gradWeight[ii] = newGWT
				}
			}
			dot := dotAcc
			for i := ii; i < normSize; i++ {
				dot += gradOutput[off+i] * weight[i] * savedInput[off+i]
				if gradWeight != nil {
					gradWeight[i] += gradOutput[off+i] * savedInput[off+i] * rrms
				}
			}
			invN := float32(1.0) / float32(normSize)
			dotCoeff := dot * rrms * rrms * rrms * invN
			vDotCoeff := float32(dotCoeff)
			vRRMS2 := float32(rrms)
			ii = 0
			for ; ii < normSize; ii++ {
				gOut := gradOutput[off+ii]
				x := savedInput[off+ii]
				gIn := gradInput[off+ii]
				w := weight[ii]
				term1 := gOut * w * vRRMS2
				term2 := x * vDotCoeff
				contribGI := term1 - term2
				result := gIn + contribGI
				gradInput[off+ii] = result
			}
			for i := ii; i < normSize; i++ {
				gradInput[off+i] += rrms*gradOutput[off+i]*weight[i] - savedInput[off+i]*dotCoeff
			}
		} else {
			for ; ii < normSize; ii++ {
				gOut := gradOutput[off+ii]
				x := savedInput[off+ii]
				contrib := gOut * x
				dotAcc = dotAcc + contrib
			}
			dot := dotAcc
			for i := ii; i < normSize; i++ {
				dot += gradOutput[off+i] * savedInput[off+i]
			}
			invN := float32(1.0) / float32(normSize)
			dotCoeff := dot * rrms * rrms * rrms * invN
			vDotCoeff := float32(dotCoeff)
			vRRMS := float32(rrms)
			ii = 0
			for ; ii < normSize; ii++ {
				gOut := gradOutput[off+ii]
				x := savedInput[off+ii]
				gIn := gradInput[off+ii]
				term1 := gOut * vRRMS
				term2 := x * vDotCoeff
				contribGI := term1 - term2
				result := gIn + contribGI
				gradInput[off+ii] = result
			}
			for i := ii; i < normSize; i++ {
				gradInput[off+i] += rrms*gradOutput[off+i] - savedInput[off+i]*dotCoeff
			}
		}
	}
}

func BaseRMSNormBackward_fallback_Float64(gradOutput []float64, savedInput []float64, savedRRMS []float64, weight []float64, normSize int, gemmaMode bool, gradInput []float64, gradWeight []float64) {
	size := min(len(gradOutput), min(len(savedInput), len(gradInput)))
	if size == 0 || normSize <= 0 {
		return
	}
	numGroups := size / normSize
	for g := range numGroups {
		off := g * normSize
		rrms := savedRRMS[g]
		dotAcc := float64(0)
		ii := 0
		if weight != nil && gemmaMode {
			vOne := float64(1.0)
			vRRMS := float64(rrms)
			for ; ii < normSize; ii++ {
				gOut := gradOutput[off+ii]
				x := savedInput[off+ii]
				w := weight[ii]
				wEff := w + vOne
				contrib := gOut * wEff * x
				dotAcc = dotAcc + contrib
				if gradWeight != nil {
					gw := gradWeight[ii]
					xRrms := x * vRRMS
					newGW := gOut*xRrms + gw
					gradWeight[ii] = newGW
				}
			}
			dot := dotAcc
			for i := ii; i < normSize; i++ {
				wEff := weight[i] + 1.0
				dot += gradOutput[off+i] * wEff * savedInput[off+i]
				if gradWeight != nil {
					gradWeight[i] += gradOutput[off+i] * savedInput[off+i] * rrms
				}
			}
			invN := float64(1.0) / float64(normSize)
			dotCoeff := dot * rrms * rrms * rrms * invN
			vDotCoeff := float64(dotCoeff)
			vRRMS2 := float64(rrms)
			ii = 0
			for ; ii < normSize; ii++ {
				gOut := gradOutput[off+ii]
				x := savedInput[off+ii]
				gIn := gradInput[off+ii]
				w := weight[ii]
				wEff := w + vOne
				term1 := gOut * wEff * vRRMS2
				term2 := x * vDotCoeff
				contribGI := term1 - term2
				result := gIn + contribGI
				gradInput[off+ii] = result
			}
			for i := ii; i < normSize; i++ {
				wEff := weight[i] + 1.0
				gradInput[off+i] += rrms*gradOutput[off+i]*wEff - savedInput[off+i]*dotCoeff
			}
		} else if weight != nil {
			vRRMS := float64(rrms)
			for ; ii < normSize; ii++ {
				gOut := gradOutput[off+ii]
				x := savedInput[off+ii]
				w := weight[ii]
				contrib := gOut * w * x
				dotAcc = dotAcc + contrib
				if gradWeight != nil {
					gwt := gradWeight[ii]
					xRrms := x * vRRMS
					newGWT := gOut*xRrms + gwt
					gradWeight[ii] = newGWT
				}
			}
			dot := dotAcc
			for i := ii; i < normSize; i++ {
				dot += gradOutput[off+i] * weight[i] * savedInput[off+i]
				if gradWeight != nil {
					gradWeight[i] += gradOutput[off+i] * savedInput[off+i] * rrms
				}
			}
			invN := float64(1.0) / float64(normSize)
			dotCoeff := dot * rrms * rrms * rrms * invN
			vDotCoeff := float64(dotCoeff)
			vRRMS2 := float64(rrms)
			ii = 0
			for ; ii < normSize; ii++ {
				gOut := gradOutput[off+ii]
				x := savedInput[off+ii]
				gIn := gradInput[off+ii]
				w := weight[ii]
				term1 := gOut * w * vRRMS2
				term2 := x * vDotCoeff
				contribGI := term1 - term2
				result := gIn + contribGI
				gradInput[off+ii] = result
			}
			for i := ii; i < normSize; i++ {
				gradInput[off+i] += rrms*gradOutput[off+i]*weight[i] - savedInput[off+i]*dotCoeff
			}
		} else {
			for ; ii < normSize; ii++ {
				gOut := gradOutput[off+ii]
				x := savedInput[off+ii]
				contrib := gOut * x
				dotAcc = dotAcc + contrib
			}
			dot := dotAcc
			for i := ii; i < normSize; i++ {
				dot += gradOutput[off+i] * savedInput[off+i]
			}
			invN := float64(1.0) / float64(normSize)
			dotCoeff := dot * rrms * rrms * rrms * invN
			vDotCoeff := float64(dotCoeff)
			vRRMS := float64(rrms)
			ii = 0
			for ; ii < normSize; ii++ {
				gOut := gradOutput[off+ii]
				x := savedInput[off+ii]
				gIn := gradInput[off+ii]
				term1 := gOut * vRRMS
				term2 := x * vDotCoeff
				contribGI := term1 - term2
				result := gIn + contribGI
				gradInput[off+ii] = result
			}
			for i := ii; i < normSize; i++ {
				gradInput[off+i] += rrms*gradOutput[off+i] - savedInput[off+i]*dotCoeff
			}
		}
	}
}
