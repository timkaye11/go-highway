// Code generated by github.com/ajroetker/go-highway/cmd/hwygen. DO NOT EDIT.

package grad

import (
	"github.com/ajroetker/go-highway/hwy"
)

func BaseReLUBackward_fallback_Float16(gradOutput []hwy.Float16, savedInput []hwy.Float16, gradInput []hwy.Float16) {
	n := min(len(gradOutput), min(len(savedInput), len(gradInput)))
	if n == 0 {
		return
	}
	vZero := hwy.Const[hwy.Float16](0.0)
	lanes := vZero.NumLanes()
	ii := 0
	for ; ii+lanes <= n; ii += lanes {
		gOut := hwy.Load(gradOutput[ii:])
		inp := hwy.Load(savedInput[ii:])
		gIn := hwy.Load(gradInput[ii:])
		mask := hwy.Greater(inp, vZero)
		masked := hwy.Merge(gOut, vZero, mask)
		result := hwy.Add(gIn, masked)
		hwy.Store(result, gradInput[ii:])
	}
	for i := ii; i < n; i++ {
		if savedInput[i].Float32() > 0 {
			gradInput[i] = hwy.Float32ToFloat16(gradInput[i].Float32() + gradOutput[i].Float32())
		}
	}
}

func BaseReLUBackward_fallback_BFloat16(gradOutput []hwy.BFloat16, savedInput []hwy.BFloat16, gradInput []hwy.BFloat16) {
	n := min(len(gradOutput), min(len(savedInput), len(gradInput)))
	if n == 0 {
		return
	}
	vZero := hwy.Const[hwy.BFloat16](0.0)
	lanes := vZero.NumLanes()
	ii := 0
	for ; ii+lanes <= n; ii += lanes {
		gOut := hwy.Load(gradOutput[ii:])
		inp := hwy.Load(savedInput[ii:])
		gIn := hwy.Load(gradInput[ii:])
		mask := hwy.Greater(inp, vZero)
		masked := hwy.Merge(gOut, vZero, mask)
		result := hwy.Add(gIn, masked)
		hwy.Store(result, gradInput[ii:])
	}
	for i := ii; i < n; i++ {
		if savedInput[i].Float32() > 0 {
			gradInput[i] = hwy.Float32ToBFloat16(gradInput[i].Float32() + gradOutput[i].Float32())
		}
	}
}

func BaseReLUBackward_fallback(gradOutput []float32, savedInput []float32, gradInput []float32) {
	n := min(len(gradOutput), min(len(savedInput), len(gradInput)))
	if n == 0 {
		return
	}
	vZero := hwy.Const[float32](0.0)
	lanes := vZero.NumLanes()
	ii := 0
	for ; ii+lanes <= n; ii += lanes {
		gOut := hwy.Load(gradOutput[ii:])
		inp := hwy.Load(savedInput[ii:])
		gIn := hwy.Load(gradInput[ii:])
		mask := hwy.Greater(inp, vZero)
		masked := hwy.Merge(gOut, vZero, mask)
		result := hwy.Add(gIn, masked)
		hwy.Store(result, gradInput[ii:])
	}
	for i := ii; i < n; i++ {
		if savedInput[i] > 0 {
			gradInput[i] += gradOutput[i]
		}
	}
}

func BaseReLUBackward_fallback_Float64(gradOutput []float64, savedInput []float64, gradInput []float64) {
	n := min(len(gradOutput), min(len(savedInput), len(gradInput)))
	if n == 0 {
		return
	}
	vZero := hwy.Set[float64](0.0)
	lanes := vZero.NumLanes()
	ii := 0
	for ; ii+lanes <= n; ii += lanes {
		gOut := hwy.Load(gradOutput[ii:])
		inp := hwy.Load(savedInput[ii:])
		gIn := hwy.Load(gradInput[ii:])
		mask := hwy.Greater(inp, vZero)
		masked := hwy.Merge(gOut, vZero, mask)
		result := hwy.Add(gIn, masked)
		hwy.Store(result, gradInput[ii:])
	}
	for i := ii; i < n; i++ {
		if savedInput[i] > 0 {
			gradInput[i] += gradOutput[i]
		}
	}
}
