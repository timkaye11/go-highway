// Code generated by github.com/ajroetker/go-highway/cmd/hwygen. DO NOT EDIT.

package grad

import (
	"github.com/ajroetker/go-highway/hwy"
)

func BaseTanhBackward_fallback_Float16(gradOutput []hwy.Float16, savedOutput []hwy.Float16, gradInput []hwy.Float16) {
	n := min(len(gradOutput), min(len(savedOutput), len(gradInput)))
	if n == 0 {
		return
	}
	vNegOne := hwy.Const[hwy.Float16](-1.0)
	lanes := vNegOne.NumLanes()
	ii := 0
	for ; ii+lanes <= n; ii += lanes {
		gOut := hwy.Load(gradOutput[ii:])
		tanhX := hwy.Load(savedOutput[ii:])
		gIn := hwy.Load(gradInput[ii:])
		gOutTanh := hwy.Mul(gOut, tanhX)
		negGOutTanh := hwy.Mul(vNegOne, gOutTanh)
		gSum := hwy.Add(gIn, gOut)
		result := hwy.MulAdd(negGOutTanh, tanhX, gSum)
		hwy.Store(result, gradInput[ii:])
	}
	for i := ii; i < n; i++ {
		tanhX := savedOutput[i]
		dTanh := float32(1) - tanhX.Float32()*tanhX.Float32()
		gradInput[i] = hwy.Float32ToFloat16(gradInput[i].Float32() + gradOutput[i].Float32()*dTanh)
	}
}

func BaseTanhBackward_fallback_BFloat16(gradOutput []hwy.BFloat16, savedOutput []hwy.BFloat16, gradInput []hwy.BFloat16) {
	n := min(len(gradOutput), min(len(savedOutput), len(gradInput)))
	if n == 0 {
		return
	}
	vNegOne := hwy.Const[hwy.BFloat16](-1.0)
	lanes := vNegOne.NumLanes()
	ii := 0
	for ; ii+lanes <= n; ii += lanes {
		gOut := hwy.Load(gradOutput[ii:])
		tanhX := hwy.Load(savedOutput[ii:])
		gIn := hwy.Load(gradInput[ii:])
		gOutTanh := hwy.Mul(gOut, tanhX)
		negGOutTanh := hwy.Mul(vNegOne, gOutTanh)
		gSum := hwy.Add(gIn, gOut)
		result := hwy.MulAdd(negGOutTanh, tanhX, gSum)
		hwy.Store(result, gradInput[ii:])
	}
	for i := ii; i < n; i++ {
		tanhX := savedOutput[i]
		dTanh := float32(1) - tanhX.Float32()*tanhX.Float32()
		gradInput[i] = hwy.Float32ToBFloat16(gradInput[i].Float32() + gradOutput[i].Float32()*dTanh)
	}
}

func BaseTanhBackward_fallback(gradOutput []float32, savedOutput []float32, gradInput []float32) {
	n := min(len(gradOutput), min(len(savedOutput), len(gradInput)))
	if n == 0 {
		return
	}
	vNegOne := float32(-1.0)
	ii := 0
	for ; ii < n; ii++ {
		gOut := gradOutput[ii]
		tanhX := savedOutput[ii]
		gIn := gradInput[ii]
		gOutTanh := gOut * tanhX
		negGOutTanh := vNegOne * gOutTanh
		gSum := gIn + gOut
		result := negGOutTanh*tanhX + gSum
		gradInput[ii] = result
	}
	for i := ii; i < n; i++ {
		tanhX := savedOutput[i]
		dTanh := float32(1) - tanhX*tanhX
		gradInput[i] += gradOutput[i] * dTanh
	}
}

func BaseTanhBackward_fallback_Float64(gradOutput []float64, savedOutput []float64, gradInput []float64) {
	n := min(len(gradOutput), min(len(savedOutput), len(gradInput)))
	if n == 0 {
		return
	}
	vNegOne := float64(-1.0)
	ii := 0
	for ; ii < n; ii++ {
		gOut := gradOutput[ii]
		tanhX := savedOutput[ii]
		gIn := gradInput[ii]
		gOutTanh := gOut * tanhX
		negGOutTanh := vNegOne * gOutTanh
		gSum := gIn + gOut
		result := negGOutTanh*tanhX + gSum
		gradInput[ii] = result
	}
	for i := ii; i < n; i++ {
		tanhX := savedOutput[i]
		dTanh := float64(1) - tanhX*tanhX
		gradInput[i] += gradOutput[i] * dTanh
	}
}
