// Code generated by github.com/ajroetker/go-highway/cmd/hwygen. DO NOT EDIT.

package grad

import (
	stdmath "math"

	"github.com/ajroetker/go-highway/hwy"
	"github.com/ajroetker/go-highway/hwy/contrib/math"
)

func BaseSwiGLUBackward_fallback_Float16(gradOutput []hwy.Float16, savedGate []hwy.Float16, savedUp []hwy.Float16, gradGate []hwy.Float16, gradUp []hwy.Float16) {
	n := min(len(gradOutput), min(len(savedGate), min(len(savedUp), min(len(gradGate), len(gradUp)))))
	if n == 0 {
		return
	}
	vOne := hwy.Const[hwy.Float16](1.0)
	lanes := vOne.NumLanes()
	ii := 0
	for ; ii+lanes <= n; ii += lanes {
		gOut := hwy.Load(gradOutput[ii:])
		g := hwy.Load(savedGate[ii:])
		u := hwy.Load(savedUp[ii:])
		gGate := hwy.Load(gradGate[ii:])
		gUp := hwy.Load(gradUp[ii:])
		sigG := math.BaseSigmoidVec_fallback_Float16(g)
		siluG := hwy.Mul(g, sigG)
		oneMinusSig := hwy.Sub(vOne, sigG)
		gTimesOMS := hwy.Mul(g, oneMinusSig)
		innerTerm := hwy.Add(vOne, gTimesOMS)
		dSilu := hwy.Mul(sigG, innerTerm)
		gOutUp := hwy.Mul(gOut, u)
		newGGate := hwy.MulAdd(gOutUp, dSilu, gGate)
		hwy.Store(newGGate, gradGate[ii:])
		newGUp := hwy.MulAdd(gOut, siluG, gUp)
		hwy.Store(newGUp, gradUp[ii:])
	}
	for i := ii; i < n; i++ {
		g := float64(savedGate[i].Float32())
		u := float64(savedUp[i].Float32())
		go_ := float64(gradOutput[i].Float32())
		sig := 1.0 / (1.0 + stdmath.Exp(-g))
		siluG := g * sig
		dSilu := sig * (1.0 + g*(1.0-sig))
		gradGate[i] = hwy.Float32ToFloat16(gradGate[i].Float32() + float32(go_*u*dSilu))
		gradUp[i] = hwy.Float32ToFloat16(gradUp[i].Float32() + float32(go_*siluG))
	}
}

func BaseSwiGLUBackward_fallback_BFloat16(gradOutput []hwy.BFloat16, savedGate []hwy.BFloat16, savedUp []hwy.BFloat16, gradGate []hwy.BFloat16, gradUp []hwy.BFloat16) {
	n := min(len(gradOutput), min(len(savedGate), min(len(savedUp), min(len(gradGate), len(gradUp)))))
	if n == 0 {
		return
	}
	vOne := hwy.Const[hwy.BFloat16](1.0)
	lanes := vOne.NumLanes()
	ii := 0
	for ; ii+lanes <= n; ii += lanes {
		gOut := hwy.Load(gradOutput[ii:])
		g := hwy.Load(savedGate[ii:])
		u := hwy.Load(savedUp[ii:])
		gGate := hwy.Load(gradGate[ii:])
		gUp := hwy.Load(gradUp[ii:])
		sigG := math.BaseSigmoidVec_fallback_BFloat16(g)
		siluG := hwy.Mul(g, sigG)
		oneMinusSig := hwy.Sub(vOne, sigG)
		gTimesOMS := hwy.Mul(g, oneMinusSig)
		innerTerm := hwy.Add(vOne, gTimesOMS)
		dSilu := hwy.Mul(sigG, innerTerm)
		gOutUp := hwy.Mul(gOut, u)
		newGGate := hwy.MulAdd(gOutUp, dSilu, gGate)
		hwy.Store(newGGate, gradGate[ii:])
		newGUp := hwy.MulAdd(gOut, siluG, gUp)
		hwy.Store(newGUp, gradUp[ii:])
	}
	for i := ii; i < n; i++ {
		g := float64(savedGate[i].Float32())
		u := float64(savedUp[i].Float32())
		go_ := float64(gradOutput[i].Float32())
		sig := 1.0 / (1.0 + stdmath.Exp(-g))
		siluG := g * sig
		dSilu := sig * (1.0 + g*(1.0-sig))
		gradGate[i] = hwy.Float32ToBFloat16(gradGate[i].Float32() + float32(go_*u*dSilu))
		gradUp[i] = hwy.Float32ToBFloat16(gradUp[i].Float32() + float32(go_*siluG))
	}
}

func BaseSwiGLUBackward_fallback(gradOutput []float32, savedGate []float32, savedUp []float32, gradGate []float32, gradUp []float32) {
	n := min(len(gradOutput), min(len(savedGate), min(len(savedUp), min(len(gradGate), len(gradUp)))))
	if n == 0 {
		return
	}
	vOne := hwy.Const[float32](1.0)
	lanes := vOne.NumLanes()
	ii := 0
	for ; ii+lanes <= n; ii += lanes {
		gOut := hwy.Load(gradOutput[ii:])
		g := hwy.Load(savedGate[ii:])
		u := hwy.Load(savedUp[ii:])
		gGate := hwy.Load(gradGate[ii:])
		gUp := hwy.Load(gradUp[ii:])
		sigG := math.BaseSigmoidVec_fallback(g)
		siluG := hwy.Mul(g, sigG)
		oneMinusSig := hwy.Sub(vOne, sigG)
		gTimesOMS := hwy.Mul(g, oneMinusSig)
		innerTerm := hwy.Add(vOne, gTimesOMS)
		dSilu := hwy.Mul(sigG, innerTerm)
		gOutUp := hwy.Mul(gOut, u)
		newGGate := hwy.MulAdd(gOutUp, dSilu, gGate)
		hwy.Store(newGGate, gradGate[ii:])
		newGUp := hwy.MulAdd(gOut, siluG, gUp)
		hwy.Store(newGUp, gradUp[ii:])
	}
	for i := ii; i < n; i++ {
		g := float64(savedGate[i])
		u := float64(savedUp[i])
		go_ := float64(gradOutput[i])
		sig := 1.0 / (1.0 + stdmath.Exp(-g))
		siluG := g * sig
		dSilu := sig * (1.0 + g*(1.0-sig))
		gradGate[i] += float32(go_ * u * dSilu)
		gradUp[i] += float32(go_ * siluG)
	}
}

func BaseSwiGLUBackward_fallback_Float64(gradOutput []float64, savedGate []float64, savedUp []float64, gradGate []float64, gradUp []float64) {
	n := min(len(gradOutput), min(len(savedGate), min(len(savedUp), min(len(gradGate), len(gradUp)))))
	if n == 0 {
		return
	}
	vOne := hwy.Set[float64](1.0)
	lanes := vOne.NumLanes()
	ii := 0
	for ; ii+lanes <= n; ii += lanes {
		gOut := hwy.Load(gradOutput[ii:])
		g := hwy.Load(savedGate[ii:])
		u := hwy.Load(savedUp[ii:])
		gGate := hwy.Load(gradGate[ii:])
		gUp := hwy.Load(gradUp[ii:])
		sigG := math.BaseSigmoidVec_fallback_Float64(g)
		siluG := hwy.Mul(g, sigG)
		oneMinusSig := hwy.Sub(vOne, sigG)
		gTimesOMS := hwy.Mul(g, oneMinusSig)
		innerTerm := hwy.Add(vOne, gTimesOMS)
		dSilu := hwy.Mul(sigG, innerTerm)
		gOutUp := hwy.Mul(gOut, u)
		newGGate := hwy.MulAdd(gOutUp, dSilu, gGate)
		hwy.Store(newGGate, gradGate[ii:])
		newGUp := hwy.MulAdd(gOut, siluG, gUp)
		hwy.Store(newGUp, gradUp[ii:])
	}
	for i := ii; i < n; i++ {
		g := float64(savedGate[i])
		u := float64(savedUp[i])
		go_ := float64(gradOutput[i])
		sig := 1.0 / (1.0 + stdmath.Exp(-g))
		siluG := g * sig
		dSilu := sig * (1.0 + g*(1.0-sig))
		gradGate[i] += float64(go_ * u * dSilu)
		gradUp[i] += float64(go_ * siluG)
	}
}
