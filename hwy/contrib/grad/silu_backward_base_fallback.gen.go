// Code generated by github.com/ajroetker/go-highway/cmd/hwygen. DO NOT EDIT.

package grad

import (
	stdmath "math"

	"github.com/ajroetker/go-highway/hwy"
	"github.com/ajroetker/go-highway/hwy/contrib/math"
)

func BaseSiLUBackward_fallback_Float16(gradOutput []hwy.Float16, savedInput []hwy.Float16, gradInput []hwy.Float16) {
	n := min(len(gradOutput), min(len(savedInput), len(gradInput)))
	if n == 0 {
		return
	}
	vOne := hwy.Const[hwy.Float16](1.0)
	lanes := vOne.NumLanes()
	ii := 0
	for ; ii+lanes <= n; ii += lanes {
		gOut := hwy.Load(gradOutput[ii:])
		x := hwy.Load(savedInput[ii:])
		gIn := hwy.Load(gradInput[ii:])
		sigX := math.BaseSigmoidVec_fallback_Float16(x)
		oneMinusSig := hwy.Sub(vOne, sigX)
		xTimesTerm := hwy.Mul(x, oneMinusSig)
		innerTerm := hwy.Add(vOne, xTimesTerm)
		dSilu := hwy.Mul(sigX, innerTerm)
		result := hwy.MulAdd(gOut, dSilu, gIn)
		hwy.Store(result, gradInput[ii:])
	}
	for i := ii; i < n; i++ {
		x := float64(savedInput[i].Float32())
		sig := 1.0 / (1.0 + stdmath.Exp(-x))
		dSilu := sig * (1.0 + x*(1.0-sig))
		gradInput[i] = hwy.Float32ToFloat16(gradInput[i].Float32() + gradOutput[i].Float32()*float32(dSilu))
	}
}

func BaseSiLUBackward_fallback_BFloat16(gradOutput []hwy.BFloat16, savedInput []hwy.BFloat16, gradInput []hwy.BFloat16) {
	n := min(len(gradOutput), min(len(savedInput), len(gradInput)))
	if n == 0 {
		return
	}
	vOne := hwy.Const[hwy.BFloat16](1.0)
	lanes := vOne.NumLanes()
	ii := 0
	for ; ii+lanes <= n; ii += lanes {
		gOut := hwy.Load(gradOutput[ii:])
		x := hwy.Load(savedInput[ii:])
		gIn := hwy.Load(gradInput[ii:])
		sigX := math.BaseSigmoidVec_fallback_BFloat16(x)
		oneMinusSig := hwy.Sub(vOne, sigX)
		xTimesTerm := hwy.Mul(x, oneMinusSig)
		innerTerm := hwy.Add(vOne, xTimesTerm)
		dSilu := hwy.Mul(sigX, innerTerm)
		result := hwy.MulAdd(gOut, dSilu, gIn)
		hwy.Store(result, gradInput[ii:])
	}
	for i := ii; i < n; i++ {
		x := float64(savedInput[i].Float32())
		sig := 1.0 / (1.0 + stdmath.Exp(-x))
		dSilu := sig * (1.0 + x*(1.0-sig))
		gradInput[i] = hwy.Float32ToBFloat16(gradInput[i].Float32() + gradOutput[i].Float32()*float32(dSilu))
	}
}

func BaseSiLUBackward_fallback(gradOutput []float32, savedInput []float32, gradInput []float32) {
	n := min(len(gradOutput), min(len(savedInput), len(gradInput)))
	if n == 0 {
		return
	}
	vOne := hwy.Const[float32](1.0)
	lanes := vOne.NumLanes()
	ii := 0
	for ; ii+lanes <= n; ii += lanes {
		gOut := hwy.Load(gradOutput[ii:])
		x := hwy.Load(savedInput[ii:])
		gIn := hwy.Load(gradInput[ii:])
		sigX := math.BaseSigmoidVec_fallback(x)
		oneMinusSig := hwy.Sub(vOne, sigX)
		xTimesTerm := hwy.Mul(x, oneMinusSig)
		innerTerm := hwy.Add(vOne, xTimesTerm)
		dSilu := hwy.Mul(sigX, innerTerm)
		result := hwy.MulAdd(gOut, dSilu, gIn)
		hwy.Store(result, gradInput[ii:])
	}
	for i := ii; i < n; i++ {
		x := float64(savedInput[i])
		sig := 1.0 / (1.0 + stdmath.Exp(-x))
		dSilu := sig * (1.0 + x*(1.0-sig))
		gradInput[i] += gradOutput[i] * float32(dSilu)
	}
}

func BaseSiLUBackward_fallback_Float64(gradOutput []float64, savedInput []float64, gradInput []float64) {
	n := min(len(gradOutput), min(len(savedInput), len(gradInput)))
	if n == 0 {
		return
	}
	vOne := hwy.Set[float64](1.0)
	lanes := vOne.NumLanes()
	ii := 0
	for ; ii+lanes <= n; ii += lanes {
		gOut := hwy.Load(gradOutput[ii:])
		x := hwy.Load(savedInput[ii:])
		gIn := hwy.Load(gradInput[ii:])
		sigX := math.BaseSigmoidVec_fallback_Float64(x)
		oneMinusSig := hwy.Sub(vOne, sigX)
		xTimesTerm := hwy.Mul(x, oneMinusSig)
		innerTerm := hwy.Add(vOne, xTimesTerm)
		dSilu := hwy.Mul(sigX, innerTerm)
		result := hwy.MulAdd(gOut, dSilu, gIn)
		hwy.Store(result, gradInput[ii:])
	}
	for i := ii; i < n; i++ {
		x := float64(savedInput[i])
		sig := 1.0 / (1.0 + stdmath.Exp(-x))
		dSilu := sig * (1.0 + x*(1.0-sig))
		gradInput[i] += gradOutput[i] * float64(dSilu)
	}
}
