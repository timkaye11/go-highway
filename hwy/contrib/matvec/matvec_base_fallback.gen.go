// Code generated by github.com/ajroetker/go-highway/cmd/hwygen. DO NOT EDIT.

package matvec

import (
	"github.com/ajroetker/go-highway/hwy"
)

func BaseMatVec_fallback_Float16(m []hwy.Float16, rows int, cols int, v []hwy.Float16, result []hwy.Float16) {
	if len(m) < rows*cols {
		panic("matrix slice too small")
	}
	if len(v) < cols {
		panic("vector slice too small")
	}
	if len(result) < rows {
		panic("result slice too small")
	}
	for i := range rows {
		row := m[i*cols : (i+1)*cols]
		sum := hwy.Zero[hwy.Float16]()
		lanes := sum.NumLanes()
		var j int
		for j = 0; j+lanes <= cols; j += lanes {
			va := hwy.Load(row[j:])
			vb := hwy.Load(v[j:])
			prod := hwy.Mul(va, vb)
			sum = hwy.Add(sum, prod)
		}
		acc := hwy.ReduceSum(sum).Float32()
		for ; j < cols; j++ {
			acc += row[j].Float32() * v[j].Float32()
		}
		result[i] = hwy.Float32ToFloat16(acc)
	}
}

func BaseMatVec_fallback_BFloat16(m []hwy.BFloat16, rows int, cols int, v []hwy.BFloat16, result []hwy.BFloat16) {
	if len(m) < rows*cols {
		panic("matrix slice too small")
	}
	if len(v) < cols {
		panic("vector slice too small")
	}
	if len(result) < rows {
		panic("result slice too small")
	}
	for i := range rows {
		row := m[i*cols : (i+1)*cols]
		sum := hwy.Zero[hwy.BFloat16]()
		lanes := sum.NumLanes()
		var j int
		for j = 0; j+lanes <= cols; j += lanes {
			va := hwy.Load(row[j:])
			vb := hwy.Load(v[j:])
			prod := hwy.Mul(va, vb)
			sum = hwy.Add(sum, prod)
		}
		acc := hwy.ReduceSum(sum).Float32()
		for ; j < cols; j++ {
			acc += row[j].Float32() * v[j].Float32()
		}
		result[i] = hwy.Float32ToBFloat16(acc)
	}
}

func BaseMatVec_fallback(m []float32, rows int, cols int, v []float32, result []float32) {
	if len(m) < rows*cols {
		panic("matrix slice too small")
	}
	if len(v) < cols {
		panic("vector slice too small")
	}
	if len(result) < rows {
		panic("result slice too small")
	}
	for i := range rows {
		row := m[i*cols : (i+1)*cols]
		sum := float32(0)
		var j int
		for j = 0; j < cols; j++ {
			va := row[j]
			vb := v[j]
			prod := va * vb
			sum = sum + prod
		}
		acc := sum
		for ; j < cols; j++ {
			acc += row[j] * v[j]
		}
		result[i] = acc
	}
}

func BaseMatVec_fallback_Float64(m []float64, rows int, cols int, v []float64, result []float64) {
	if len(m) < rows*cols {
		panic("matrix slice too small")
	}
	if len(v) < cols {
		panic("vector slice too small")
	}
	if len(result) < rows {
		panic("result slice too small")
	}
	for i := range rows {
		row := m[i*cols : (i+1)*cols]
		sum := float64(0)
		var j int
		for j = 0; j < cols; j++ {
			va := row[j]
			vb := v[j]
			prod := va * vb
			sum = sum + prod
		}
		acc := sum
		for ; j < cols; j++ {
			acc += row[j] * v[j]
		}
		result[i] = acc
	}
}
