// Code generated by hwygen. DO NOT EDIT.
//go:build amd64 && goexperiment.simd

package matmul

import (
	"github.com/ajroetker/go-highway/hwy"
	"simd/archsimd"
)

func BaseMatMul_avx2_Float16(a []hwy.Float16, b []hwy.Float16, c []hwy.Float16, m int, n int, k int) {
	if len(a) < m*k {
		panic("matmul: A slice too short")
	}
	if len(b) < k*n {
		panic("matmul: B slice too short")
	}
	if len(c) < m*n {
		panic("matmul: C slice too short")
	}
	for i := range m {
		cRow := c[i*n : (i+1)*n]
		vZero := hwy.Zero[hwy.Float16]()
		lanes := 16
		var j int
		for j = 0; j+lanes <= n; j += lanes {
			hwy.Store(vZero, cRow[j:])
		}
		for ; j < n; j++ {
			cRow[j] = hwy.Float32ToFloat16(0)
		}
		for p := range k {
			aip := a[i*k+p]
			vA := hwy.Set(aip)
			bRow := b[p*n : (p+1)*n]
			for j = 0; j+lanes <= n; j += lanes {
				vB := hwy.Load(bRow[j:])
				vC := hwy.Load(cRow[j:])
				vC = hwy.FMAF16(vA, vB, vC)
				hwy.Store(vC, cRow[j:])
			}
			for ; j < n; j++ {
				cRow[j] = hwy.Float32ToFloat16(cRow[j].Float32() + aip.Float32()*bRow[j].Float32())
			}
		}
	}
}

func BaseMatMul_avx2_BFloat16(a []hwy.BFloat16, b []hwy.BFloat16, c []hwy.BFloat16, m int, n int, k int) {
	if len(a) < m*k {
		panic("matmul: A slice too short")
	}
	if len(b) < k*n {
		panic("matmul: B slice too short")
	}
	if len(c) < m*n {
		panic("matmul: C slice too short")
	}
	for i := range m {
		cRow := c[i*n : (i+1)*n]
		vZero := hwy.Zero[hwy.BFloat16]()
		lanes := 16
		var j int
		for j = 0; j+lanes <= n; j += lanes {
			hwy.Store(vZero, cRow[j:])
		}
		for ; j < n; j++ {
			cRow[j] = hwy.Float32ToBFloat16(0)
		}
		for p := range k {
			aip := a[i*k+p]
			vA := hwy.Set(aip)
			bRow := b[p*n : (p+1)*n]
			for j = 0; j+lanes <= n; j += lanes {
				vB := hwy.Load(bRow[j:])
				vC := hwy.Load(cRow[j:])
				vC = hwy.FMABF16(vA, vB, vC)
				hwy.Store(vC, cRow[j:])
			}
			for ; j < n; j++ {
				cRow[j] = hwy.Float32ToBFloat16(cRow[j].Float32() + aip.Float32()*bRow[j].Float32())
			}
		}
	}
}

func BaseMatMul_avx2(a []float32, b []float32, c []float32, m int, n int, k int) {
	if len(a) < m*k {
		panic("matmul: A slice too short")
	}
	if len(b) < k*n {
		panic("matmul: B slice too short")
	}
	if len(c) < m*n {
		panic("matmul: C slice too short")
	}
	for i := range m {
		cRow := c[i*n : (i+1)*n]
		vZero := archsimd.BroadcastFloat32x8(0)
		lanes := 8
		var j int
		for j = 0; j+lanes <= n; j += lanes {
			vZero.StoreSlice(cRow[j:])
		}
		for ; j < n; j++ {
			cRow[j] = 0
		}
		for p := range k {
			aip := a[i*k+p]
			vA := archsimd.BroadcastFloat32x8(aip)
			bRow := b[p*n : (p+1)*n]
			for j = 0; j+lanes <= n; j += lanes {
				vB := archsimd.LoadFloat32x8Slice(bRow[j:])
				vC := archsimd.LoadFloat32x8Slice(cRow[j:])
				vC = vA.MulAdd(vB, vC)
				vC.StoreSlice(cRow[j:])
			}
			for ; j < n; j++ {
				cRow[j] += aip * bRow[j]
			}
		}
	}
}

func BaseMatMul_avx2_Float64(a []float64, b []float64, c []float64, m int, n int, k int) {
	if len(a) < m*k {
		panic("matmul: A slice too short")
	}
	if len(b) < k*n {
		panic("matmul: B slice too short")
	}
	if len(c) < m*n {
		panic("matmul: C slice too short")
	}
	for i := range m {
		cRow := c[i*n : (i+1)*n]
		vZero := archsimd.BroadcastFloat64x4(0)
		lanes := 4
		var j int
		for j = 0; j+lanes <= n; j += lanes {
			vZero.StoreSlice(cRow[j:])
		}
		for ; j < n; j++ {
			cRow[j] = 0
		}
		for p := range k {
			aip := a[i*k+p]
			vA := archsimd.BroadcastFloat64x4(aip)
			bRow := b[p*n : (p+1)*n]
			for j = 0; j+lanes <= n; j += lanes {
				vB := archsimd.LoadFloat64x4Slice(bRow[j:])
				vC := archsimd.LoadFloat64x4Slice(cRow[j:])
				vC = vA.MulAdd(vB, vC)
				vC.StoreSlice(cRow[j:])
			}
			for ; j < n; j++ {
				cRow[j] += aip * bRow[j]
			}
		}
	}
}
