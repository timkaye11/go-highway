// Code generated by github.com/ajroetker/go-highway/cmd/hwygen. DO NOT EDIT.

//go:build amd64 && goexperiment.simd

package matmul

import (
	"simd/archsimd"
	"unsafe"

	"github.com/ajroetker/go-highway/hwy"
	"github.com/ajroetker/go-highway/hwy/asm"
)

func BaseMatMulKLast_avx2_Float16(a []hwy.Float16, b []hwy.Float16, c []hwy.Float16, m int, n int, k int) {
	if len(a) < m*k {
		panic("matmul: A slice too short")
	}
	if len(b) < n*k {
		panic("matmul: B slice too short")
	}
	if len(c) < m*n {
		panic("matmul: C slice too short")
	}
	lanes := 8
	var i int
	for i = 0; i+3 < m; i += 4 {
		aRow0 := i * k
		aRow1 := (i + 1) * k
		aRow2 := (i + 2) * k
		aRow3 := (i + 3) * k
		cRow0 := i * n
		cRow1 := (i + 1) * n
		cRow2 := (i + 2) * n
		cRow3 := (i + 3) * n
		for j := 0; j < n; j++ {
			bRow := j * k
			acc0 := asm.ZeroFloat16x8AVX2()
			acc1 := asm.ZeroFloat16x8AVX2()
			acc2 := asm.ZeroFloat16x8AVX2()
			acc3 := asm.ZeroFloat16x8AVX2()
			var p int
			for p = 0; p+lanes <= k; p += lanes {
				vB := asm.LoadFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(b[bRow+p:]))), len(b[bRow+p:])))
				vA0 := asm.LoadFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(a[aRow0+p:]))), len(a[aRow0+p:])))
				vA1 := asm.LoadFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(a[aRow1+p:]))), len(a[aRow1+p:])))
				vA2 := asm.LoadFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(a[aRow2+p:]))), len(a[aRow2+p:])))
				vA3 := asm.LoadFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(a[aRow3+p:]))), len(a[aRow3+p:])))
				acc0 = vA0.MulAdd(vB, acc0)
				acc1 = vA1.MulAdd(vB, acc1)
				acc2 = vA2.MulAdd(vB, acc2)
				acc3 = vA3.MulAdd(vB, acc3)
			}
			sum0 := acc0.ReduceSum()
			sum1 := acc1.ReduceSum()
			sum2 := acc2.ReduceSum()
			sum3 := acc3.ReduceSum()
			for ; p < k; p++ {
				sum0 += a[aRow0+p].Float32() * b[bRow+p].Float32()
				sum1 += a[aRow1+p].Float32() * b[bRow+p].Float32()
				sum2 += a[aRow2+p].Float32() * b[bRow+p].Float32()
				sum3 += a[aRow3+p].Float32() * b[bRow+p].Float32()
			}
			c[cRow0+j] = hwy.Float32ToFloat16(sum0)
			c[cRow1+j] = hwy.Float32ToFloat16(sum1)
			c[cRow2+j] = hwy.Float32ToFloat16(sum2)
			c[cRow3+j] = hwy.Float32ToFloat16(sum3)
		}
	}
	for ; i < m; i++ {
		aRow := i * k
		cRow := i * n
		for j := 0; j < n; j++ {
			bRow := j * k
			acc := asm.ZeroFloat16x8AVX2()
			var p int
			for p = 0; p+lanes <= k; p += lanes {
				vA := asm.LoadFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(a[aRow+p:]))), len(a[aRow+p:])))
				vB := asm.LoadFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(b[bRow+p:]))), len(b[bRow+p:])))
				acc = vA.MulAdd(vB, acc)
			}
			sum := acc.ReduceSum()
			for ; p < k; p++ {
				sum += a[aRow+p].Float32() * b[bRow+p].Float32()
			}
			c[cRow+j] = hwy.Float32ToFloat16(sum)
		}
	}
}

func BaseMatMulKLast_avx2_BFloat16(a []hwy.BFloat16, b []hwy.BFloat16, c []hwy.BFloat16, m int, n int, k int) {
	if len(a) < m*k {
		panic("matmul: A slice too short")
	}
	if len(b) < n*k {
		panic("matmul: B slice too short")
	}
	if len(c) < m*n {
		panic("matmul: C slice too short")
	}
	lanes := 8
	var i int
	for i = 0; i+3 < m; i += 4 {
		aRow0 := i * k
		aRow1 := (i + 1) * k
		aRow2 := (i + 2) * k
		aRow3 := (i + 3) * k
		cRow0 := i * n
		cRow1 := (i + 1) * n
		cRow2 := (i + 2) * n
		cRow3 := (i + 3) * n
		for j := 0; j < n; j++ {
			bRow := j * k
			acc0 := asm.ZeroBFloat16x8AVX2()
			acc1 := asm.ZeroBFloat16x8AVX2()
			acc2 := asm.ZeroBFloat16x8AVX2()
			acc3 := asm.ZeroBFloat16x8AVX2()
			var p int
			for p = 0; p+lanes <= k; p += lanes {
				vB := asm.LoadBFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(b[bRow+p:]))), len(b[bRow+p:])))
				vA0 := asm.LoadBFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(a[aRow0+p:]))), len(a[aRow0+p:])))
				vA1 := asm.LoadBFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(a[aRow1+p:]))), len(a[aRow1+p:])))
				vA2 := asm.LoadBFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(a[aRow2+p:]))), len(a[aRow2+p:])))
				vA3 := asm.LoadBFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(a[aRow3+p:]))), len(a[aRow3+p:])))
				acc0 = vA0.MulAdd(vB, acc0)
				acc1 = vA1.MulAdd(vB, acc1)
				acc2 = vA2.MulAdd(vB, acc2)
				acc3 = vA3.MulAdd(vB, acc3)
			}
			sum0 := acc0.ReduceSum()
			sum1 := acc1.ReduceSum()
			sum2 := acc2.ReduceSum()
			sum3 := acc3.ReduceSum()
			for ; p < k; p++ {
				sum0 += a[aRow0+p].Float32() * b[bRow+p].Float32()
				sum1 += a[aRow1+p].Float32() * b[bRow+p].Float32()
				sum2 += a[aRow2+p].Float32() * b[bRow+p].Float32()
				sum3 += a[aRow3+p].Float32() * b[bRow+p].Float32()
			}
			c[cRow0+j] = hwy.Float32ToBFloat16(sum0)
			c[cRow1+j] = hwy.Float32ToBFloat16(sum1)
			c[cRow2+j] = hwy.Float32ToBFloat16(sum2)
			c[cRow3+j] = hwy.Float32ToBFloat16(sum3)
		}
	}
	for ; i < m; i++ {
		aRow := i * k
		cRow := i * n
		for j := 0; j < n; j++ {
			bRow := j * k
			acc := asm.ZeroBFloat16x8AVX2()
			var p int
			for p = 0; p+lanes <= k; p += lanes {
				vA := asm.LoadBFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(a[aRow+p:]))), len(a[aRow+p:])))
				vB := asm.LoadBFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(b[bRow+p:]))), len(b[bRow+p:])))
				acc = vA.MulAdd(vB, acc)
			}
			sum := acc.ReduceSum()
			for ; p < k; p++ {
				sum += a[aRow+p].Float32() * b[bRow+p].Float32()
			}
			c[cRow+j] = hwy.Float32ToBFloat16(sum)
		}
	}
}

func BaseMatMulKLast_avx2(a []float32, b []float32, c []float32, m int, n int, k int) {
	if len(a) < m*k {
		panic("matmul: A slice too short")
	}
	if len(b) < n*k {
		panic("matmul: B slice too short")
	}
	if len(c) < m*n {
		panic("matmul: C slice too short")
	}
	lanes := 8
	var i int
	for i = 0; i+3 < m; i += 4 {
		aRow0 := i * k
		aRow1 := (i + 1) * k
		aRow2 := (i + 2) * k
		aRow3 := (i + 3) * k
		cRow0 := i * n
		cRow1 := (i + 1) * n
		cRow2 := (i + 2) * n
		cRow3 := (i + 3) * n
		for j := 0; j < n; j++ {
			bRow := j * k
			acc0 := archsimd.BroadcastFloat32x8(0)
			acc1 := archsimd.BroadcastFloat32x8(0)
			acc2 := archsimd.BroadcastFloat32x8(0)
			acc3 := archsimd.BroadcastFloat32x8(0)
			var p int
			for p = 0; p+lanes <= k; p += lanes {
				vB := archsimd.LoadFloat32x8Slice(b[bRow+p:])
				vA0 := archsimd.LoadFloat32x8Slice(a[aRow0+p:])
				vA1 := archsimd.LoadFloat32x8Slice(a[aRow1+p:])
				vA2 := archsimd.LoadFloat32x8Slice(a[aRow2+p:])
				vA3 := archsimd.LoadFloat32x8Slice(a[aRow3+p:])
				acc0 = vA0.MulAdd(vB, acc0)
				acc1 = vA1.MulAdd(vB, acc1)
				acc2 = vA2.MulAdd(vB, acc2)
				acc3 = vA3.MulAdd(vB, acc3)
			}
			sum0 := hwy.ReduceSum_AVX2_F32x8(acc0)
			sum1 := hwy.ReduceSum_AVX2_F32x8(acc1)
			sum2 := hwy.ReduceSum_AVX2_F32x8(acc2)
			sum3 := hwy.ReduceSum_AVX2_F32x8(acc3)
			for ; p < k; p++ {
				sum0 += a[aRow0+p] * b[bRow+p]
				sum1 += a[aRow1+p] * b[bRow+p]
				sum2 += a[aRow2+p] * b[bRow+p]
				sum3 += a[aRow3+p] * b[bRow+p]
			}
			c[cRow0+j] = sum0
			c[cRow1+j] = sum1
			c[cRow2+j] = sum2
			c[cRow3+j] = sum3
		}
	}
	for ; i < m; i++ {
		aRow := i * k
		cRow := i * n
		for j := 0; j < n; j++ {
			bRow := j * k
			acc := archsimd.BroadcastFloat32x8(0)
			var p int
			for p = 0; p+lanes <= k; p += lanes {
				vA := archsimd.LoadFloat32x8Slice(a[aRow+p:])
				vB := archsimd.LoadFloat32x8Slice(b[bRow+p:])
				acc = vA.MulAdd(vB, acc)
			}
			sum := hwy.ReduceSum_AVX2_F32x8(acc)
			for ; p < k; p++ {
				sum += a[aRow+p] * b[bRow+p]
			}
			c[cRow+j] = sum
		}
	}
}

func BaseMatMulKLast_avx2_Float64(a []float64, b []float64, c []float64, m int, n int, k int) {
	if len(a) < m*k {
		panic("matmul: A slice too short")
	}
	if len(b) < n*k {
		panic("matmul: B slice too short")
	}
	if len(c) < m*n {
		panic("matmul: C slice too short")
	}
	lanes := 4
	var i int
	for i = 0; i+3 < m; i += 4 {
		aRow0 := i * k
		aRow1 := (i + 1) * k
		aRow2 := (i + 2) * k
		aRow3 := (i + 3) * k
		cRow0 := i * n
		cRow1 := (i + 1) * n
		cRow2 := (i + 2) * n
		cRow3 := (i + 3) * n
		for j := 0; j < n; j++ {
			bRow := j * k
			acc0 := archsimd.BroadcastFloat64x4(0)
			acc1 := archsimd.BroadcastFloat64x4(0)
			acc2 := archsimd.BroadcastFloat64x4(0)
			acc3 := archsimd.BroadcastFloat64x4(0)
			var p int
			for p = 0; p+lanes <= k; p += lanes {
				vB := archsimd.LoadFloat64x4Slice(b[bRow+p:])
				vA0 := archsimd.LoadFloat64x4Slice(a[aRow0+p:])
				vA1 := archsimd.LoadFloat64x4Slice(a[aRow1+p:])
				vA2 := archsimd.LoadFloat64x4Slice(a[aRow2+p:])
				vA3 := archsimd.LoadFloat64x4Slice(a[aRow3+p:])
				acc0 = vA0.MulAdd(vB, acc0)
				acc1 = vA1.MulAdd(vB, acc1)
				acc2 = vA2.MulAdd(vB, acc2)
				acc3 = vA3.MulAdd(vB, acc3)
			}
			sum0 := hwy.ReduceSum_AVX2_F64x4(acc0)
			sum1 := hwy.ReduceSum_AVX2_F64x4(acc1)
			sum2 := hwy.ReduceSum_AVX2_F64x4(acc2)
			sum3 := hwy.ReduceSum_AVX2_F64x4(acc3)
			for ; p < k; p++ {
				sum0 += a[aRow0+p] * b[bRow+p]
				sum1 += a[aRow1+p] * b[bRow+p]
				sum2 += a[aRow2+p] * b[bRow+p]
				sum3 += a[aRow3+p] * b[bRow+p]
			}
			c[cRow0+j] = sum0
			c[cRow1+j] = sum1
			c[cRow2+j] = sum2
			c[cRow3+j] = sum3
		}
	}
	for ; i < m; i++ {
		aRow := i * k
		cRow := i * n
		for j := 0; j < n; j++ {
			bRow := j * k
			acc := archsimd.BroadcastFloat64x4(0)
			var p int
			for p = 0; p+lanes <= k; p += lanes {
				vA := archsimd.LoadFloat64x4Slice(a[aRow+p:])
				vB := archsimd.LoadFloat64x4Slice(b[bRow+p:])
				acc = vA.MulAdd(vB, acc)
			}
			sum := hwy.ReduceSum_AVX2_F64x4(acc)
			for ; p < k; p++ {
				sum += a[aRow+p] * b[bRow+p]
			}
			c[cRow+j] = sum
		}
	}
}

func BaseMatMulKLastBlocked_avx2_Float16(a []hwy.Float16, b []hwy.Float16, c []hwy.Float16, m int, n int, k int) {
	if len(a) < m*k {
		panic("matmul: A slice too short")
	}
	if len(b) < n*k {
		panic("matmul: B slice too short")
	}
	if len(c) < m*n {
		panic("matmul: C slice too short")
	}
	const blockM = 64
	const blockN = 64
	const blockK = 256
	lanes := 8
	for i := range c[:m*n] {
		c[i] = hwy.Float32ToFloat16(0)
	}
	for ii := 0; ii < m; ii += blockM {
		iEnd := min(ii+blockM, m)
		for jj := 0; jj < n; jj += blockN {
			jEnd := min(jj+blockN, n)
			for kk := 0; kk < k; kk += blockK {
				kEnd := min(kk+blockK, k)
				for i := ii; i < iEnd; i++ {
					aRow := i * k
					cRow := i * n
					for j := jj; j < jEnd; j++ {
						bRow := j * k
						acc := asm.ZeroFloat16x8AVX2()
						var p int
						for p = kk; p+lanes <= kEnd; p += lanes {
							vA := asm.LoadFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(a[aRow+p:]))), len(a[aRow+p:])))
							vB := asm.LoadFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(b[bRow+p:]))), len(b[bRow+p:])))
							acc = vA.MulAdd(vB, acc)
						}
						sum := acc.ReduceSum()
						for ; p < kEnd; p++ {
							sum += a[aRow+p].Float32() * b[bRow+p].Float32()
						}
						c[cRow+j] = hwy.Float32ToFloat16(c[cRow+j].Float32() + sum)
					}
				}
			}
		}
	}
}

func BaseMatMulKLastBlocked_avx2_BFloat16(a []hwy.BFloat16, b []hwy.BFloat16, c []hwy.BFloat16, m int, n int, k int) {
	if len(a) < m*k {
		panic("matmul: A slice too short")
	}
	if len(b) < n*k {
		panic("matmul: B slice too short")
	}
	if len(c) < m*n {
		panic("matmul: C slice too short")
	}
	const blockM = 64
	const blockN = 64
	const blockK = 256
	lanes := 8
	for i := range c[:m*n] {
		c[i] = hwy.Float32ToBFloat16(0)
	}
	for ii := 0; ii < m; ii += blockM {
		iEnd := min(ii+blockM, m)
		for jj := 0; jj < n; jj += blockN {
			jEnd := min(jj+blockN, n)
			for kk := 0; kk < k; kk += blockK {
				kEnd := min(kk+blockK, k)
				for i := ii; i < iEnd; i++ {
					aRow := i * k
					cRow := i * n
					for j := jj; j < jEnd; j++ {
						bRow := j * k
						acc := asm.ZeroBFloat16x8AVX2()
						var p int
						for p = kk; p+lanes <= kEnd; p += lanes {
							vA := asm.LoadBFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(a[aRow+p:]))), len(a[aRow+p:])))
							vB := asm.LoadBFloat16x8AVX2Slice(unsafe.Slice((*uint16)(unsafe.Pointer(unsafe.SliceData(b[bRow+p:]))), len(b[bRow+p:])))
							acc = vA.MulAdd(vB, acc)
						}
						sum := acc.ReduceSum()
						for ; p < kEnd; p++ {
							sum += a[aRow+p].Float32() * b[bRow+p].Float32()
						}
						c[cRow+j] = hwy.Float32ToBFloat16(c[cRow+j].Float32() + sum)
					}
				}
			}
		}
	}
}

func BaseMatMulKLastBlocked_avx2(a []float32, b []float32, c []float32, m int, n int, k int) {
	if len(a) < m*k {
		panic("matmul: A slice too short")
	}
	if len(b) < n*k {
		panic("matmul: B slice too short")
	}
	if len(c) < m*n {
		panic("matmul: C slice too short")
	}
	const blockM = 64
	const blockN = 64
	const blockK = 256
	lanes := 8
	for i := range c[:m*n] {
		c[i] = 0
	}
	for ii := 0; ii < m; ii += blockM {
		iEnd := min(ii+blockM, m)
		for jj := 0; jj < n; jj += blockN {
			jEnd := min(jj+blockN, n)
			for kk := 0; kk < k; kk += blockK {
				kEnd := min(kk+blockK, k)
				for i := ii; i < iEnd; i++ {
					aRow := i * k
					cRow := i * n
					for j := jj; j < jEnd; j++ {
						bRow := j * k
						acc := archsimd.BroadcastFloat32x8(0)
						var p int
						for p = kk; p+lanes <= kEnd; p += lanes {
							vA := archsimd.LoadFloat32x8Slice(a[aRow+p:])
							vB := archsimd.LoadFloat32x8Slice(b[bRow+p:])
							acc = vA.MulAdd(vB, acc)
						}
						sum := hwy.ReduceSum_AVX2_F32x8(acc)
						for ; p < kEnd; p++ {
							sum += a[aRow+p] * b[bRow+p]
						}
						c[cRow+j] += sum
					}
				}
			}
		}
	}
}

func BaseMatMulKLastBlocked_avx2_Float64(a []float64, b []float64, c []float64, m int, n int, k int) {
	if len(a) < m*k {
		panic("matmul: A slice too short")
	}
	if len(b) < n*k {
		panic("matmul: B slice too short")
	}
	if len(c) < m*n {
		panic("matmul: C slice too short")
	}
	const blockM = 64
	const blockN = 64
	const blockK = 256
	lanes := 4
	for i := range c[:m*n] {
		c[i] = 0
	}
	for ii := 0; ii < m; ii += blockM {
		iEnd := min(ii+blockM, m)
		for jj := 0; jj < n; jj += blockN {
			jEnd := min(jj+blockN, n)
			for kk := 0; kk < k; kk += blockK {
				kEnd := min(kk+blockK, k)
				for i := ii; i < iEnd; i++ {
					aRow := i * k
					cRow := i * n
					for j := jj; j < jEnd; j++ {
						bRow := j * k
						acc := archsimd.BroadcastFloat64x4(0)
						var p int
						for p = kk; p+lanes <= kEnd; p += lanes {
							vA := archsimd.LoadFloat64x4Slice(a[aRow+p:])
							vB := archsimd.LoadFloat64x4Slice(b[bRow+p:])
							acc = vA.MulAdd(vB, acc)
						}
						sum := hwy.ReduceSum_AVX2_F64x4(acc)
						for ; p < kEnd; p++ {
							sum += a[aRow+p] * b[bRow+p]
						}
						c[cRow+j] += sum
					}
				}
			}
		}
	}
}
